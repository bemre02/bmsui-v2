<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
    <Product>BMS UI</Product>
    <AssemblyTitle>BMS UI</AssemblyTitle>
    <Description>Formula Student HV BMS desktop interface</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.IO.Ports" Version="9.0.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Testlerin UI veri yolunu (Start -> simulasyon -> etiketler) surebilmesi icin -->
    <InternalsVisibleTo Include="BmsUi.Tests" />
  </ItemGroup>

  <ItemGroup>
    <EmbeddedResource Include="Resources\team_logo.png" />
  </ItemGroup>

</Project>