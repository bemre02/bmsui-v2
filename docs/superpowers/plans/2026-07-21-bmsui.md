# BmsUi Masaüstü UI — Uygulama Planı

> **Agentic worker'lar için:** GEREKLİ ALT-SKILL: Bu planı görev görev uygulamak için
> `superpowers:executing-plans` kullanın. Adımlar checkbox (`- [ ]`) sözdizimiyle takip edilir.

**Hedef:** HV BMS master kartına (STM32G474 + FreeRTOS) USB CDC üzerinden bağlanıp 96 hücrenin
voltaj/sıcaklığını, paket verilerini, fault/balans/kontaktör durumunu gösteren .NET 10 WinForms
masaüstü uygulaması.

**Mimari:** Katmanlı — `ISerialTransport` (I/O soyutlaması) → `SerialLink` (komut-cevap +
CRC doğrulama) → `FrameParser` (binary → tipli veri) → `PollWorker` (katmanlı zamanlama,
arka plan thread) → `BmsSnapshot` (değişmez durum) → `Form1` (UI, `Invoke` ile güncellenir).
Portu yalnızca worker thread'i kullanır; UI'den gelen yazma istekleri kuyruğa girer.

**Tech Stack:** C# / .NET 10 (`net10.0-windows`), WinForms, `System.IO.Ports` 9.x, xUnit,
Python 3.13 + pyserial 3.5 (simülatör).

## Global Kısıtlar

- Hedef çatı: `net10.0-windows`, `UseWindowsForms=true`, `Nullable=enable`, `ImplicitUsings=enable`.
- `System.IO.Ports` sürüm 9.x (lvbmsgui ile aynı).
- **`serialPort.ReadLine()` asla kullanılmaz** — protokol binary, satır sonu yok.
- **Aynı anda tek transaction.** Komut tek `Write` çağrısıyla yazılır; cevabın tamamı
  okunmadan yeni komut gönderilmez (firmware `switch (usblen)` ile paket uzunluğuna göre
  ayrıştırıyor — `main.cpp:1958`).
- Portu yalnızca `PollWorker` thread'i kullanır. UI asla doğrudan porta yazmaz.
- CRC8 = CRC-8/SMBUS: poly `0x07`, init `0x00`, refleksiyon yok, xorout `0x00`; çerçevenin
  **son baytı hariç** tümü üzerinde.
- İşaretli alanlar: hücre sıcaklıkları (int16), `PACK_CURRENT` (idx 8), `PACK_MAX/MIN_CELL_TEMP`
  (idx 12/13), `PACK_MAX_SLAVE_TEMP` (idx 16). Bunlar `ReadInt16LittleEndian` ile okunur.
- MAINBUFFER indeksi `< 50` olmalı; `41/42/43` özel komutlarla gölgeli, **register olarak asla
  sorgulanmaz**. Güç host'ta `V × I` ile hesaplanır.
- UI metinleri Türkçe; sınıf/metot/değişken adları İngilizce; kod yorumları Türkçe.
- Her görev sonunda `dotnet build -c Release` ve `dotnet test` temiz geçmeli.
- Proje kökü: `C:\Users\burem\STM32CubeIDE\workspace_1.19.0\bmsui`

## Dosya Yapısı

| Dosya | Sorumluluk |
|---|---|
| `BmsUi/Protocol/Crc8.cs` | CRC-8/SMBUS hesabı — tek public metot |
| `BmsUi/Protocol/HvProtocol.cs` | Komut baytları, çerçeve uzunlukları, MAINBUFFER indeksleri, fault bit adları |
| `BmsUi/Protocol/FrameParser.cs` | 194/14/4 baytlık çerçeveleri doğrulayıp tipli veriye çevirir |
| `BmsUi/Serial/ISerialTransport.cs` | Aç/kapa/oku/yaz soyutlaması (test için sahte uygulama) |
| `BmsUi/Serial/SerialPortTransport.cs` | `System.IO.Ports.SerialPort` sarmalayıcı |
| `BmsUi/Serial/SerialLink.cs` | `Transact`, `Ping`, register oku/yaz, hata sayaçları |
| `BmsUi/Model/BmsSnapshot.cs` | Değişmez tam durum + türetilmiş büyüklükler |
| `BmsUi/Model/CellStats.cs` | min/max/avg + indeks hesabı, geçerlilik filtresi |
| `BmsUi/Polling/PollSchedule.cs` | Saf zamanlama kararı: tick numarası → çekilecek öğeler |
| `BmsUi/Polling/PollWorker.cs` | Arka plan thread, yazma kuyruğu, snapshot yayını |
| `BmsUi/Ui/Heatmap.cs` | Değer → renk eşlemesi (saf, test edilebilir) |
| `BmsUi/Ui/CellGridControl.cs` | Owner-drawn 6×16 hücre ızgarası |
| `BmsUi/Ui/StatusLight.cs` | AIR/PRE/ERR ışığı |
| `BmsUi/Logging/CsvLogger.cs` | CSV başlığı + satır yazımı, hız sınırlama |
| `BmsUi/Form1.cs` / `.Designer.cs` | Yerleşim, olay bağlama, `Invoke` ile güncelleme |
| `bms_simulator.py` | Donanımsız uçtan uca test |
| `BmsUi.Tests/*` | xUnit testleri |

---

### Görev 1: Proje iskeleti, git, çözüm

**Dosyalar:**
- Oluştur: `bmsui/.gitignore`, `BmsUi.sln`, `BmsUi/BmsUi.csproj`, `BmsUi/Program.cs`,
  `BmsUi/Form1.cs`, `BmsUi/Form1.Designer.cs`, `BmsUi.Tests/BmsUi.Tests.csproj`

**Arayüzler:**
- Üretir: derlenebilir boş WinForms uygulaması + çalışan `dotnet test` altyapısı.

- [ ] **Adım 1: git deposu ve .gitignore**

```bash
cd "C:/Users/burem/STM32CubeIDE/workspace_1.19.0/bmsui"
git init
```

`.gitignore` içeriği:

```gitignore
bin/
obj/
*.user
.vs/
__pycache__/
*.csv
```

- [ ] **Adım 2: Projeleri oluştur**

```bash
cd "C:/Users/burem/STM32CubeIDE/workspace_1.19.0/bmsui"
dotnet new winforms -n BmsUi -f net10.0
dotnet new xunit -n BmsUi.Tests -f net10.0
dotnet new sln -n BmsUi
dotnet sln add BmsUi/BmsUi.csproj BmsUi.Tests/BmsUi.Tests.csproj
dotnet add BmsUi.Tests/BmsUi.Tests.csproj reference BmsUi/BmsUi.csproj
dotnet add BmsUi/BmsUi.csproj package System.IO.Ports --version 9.0.0
```

- [ ] **Adım 3: `BmsUi.Tests.csproj`'u WinForms'a uyumla**

Test projesi WinForms tipi referans aldığı için hedef çatı ve `UseWindowsForms` eşitlenmeli:

```xml
<TargetFramework>net10.0-windows</TargetFramework>
<UseWindowsForms>true</UseWindowsForms>
```

- [ ] **Adım 4: Derle ve testi çalıştır**

Çalıştır: `dotnet build -c Release` ve `dotnet test`
Beklenen: build başarılı; test çıktısı `Passed! - Failed: 0, Passed: 0` (henüz test yok).

- [ ] **Adım 5: Commit**

```bash
git add -A
git commit -m "chore: BmsUi cozum iskeleti (WinForms + xUnit)"
```

---

### Görev 2: CRC-8/SMBUS

**Dosyalar:**
- Oluştur: `BmsUi/Protocol/Crc8.cs`, `BmsUi.Tests/Crc8Tests.cs`

**Arayüzler:**
- Üretir: `static byte Crc8.Compute(ReadOnlySpan<byte> data)`

- [ ] **Adım 1: Başarısız testi yaz** — `BmsUi.Tests/Crc8Tests.cs`

```csharp
using BmsUi.Protocol;
using Xunit;

public class Crc8Tests
{
    [Fact]
    public void Compute_StandardCheckVector_Returns0xF4()
    {
        // CRC-8/SMBUS'in resmi "check" degeri: "123456789" -> 0xF4
        byte[] data = System.Text.Encoding.ASCII.GetBytes("123456789");
        Assert.Equal(0xF4, Crc8.Compute(data));
    }

    [Fact]
    public void Compute_EmptyInput_ReturnsZero()
        => Assert.Equal(0x00, Crc8.Compute(System.Array.Empty<byte>()));

    [Fact]
    public void Compute_SingleZeroByte_ReturnsZero()
        => Assert.Equal(0x00, Crc8.Compute(new byte[] { 0x00 }));

    [Fact]
    public void Compute_SingleByte0x29_Returns0x9E()
    {
        // Firmware algoritmasi (main.cpp:2119) ile elle hesaplanmis deger
        Assert.Equal(0x9E, Crc8.Compute(new byte[] { 0x29 }));
    }
}
```

- [ ] **Adım 2: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter Crc8Tests`
Beklenen: derleme hatası — `Crc8` bulunamadı.

- [ ] **Adım 3: Uygulamayı yaz** — `BmsUi/Protocol/Crc8.cs`

```csharp
namespace BmsUi.Protocol;

/// <summary>
/// CRC-8/SMBUS: poly=0x07, init=0x00, refleksiyon yok, xorout=0x00.
/// Firmware karsiligi: main.cpp calculateCRC8().
/// </summary>
public static class Crc8
{
    public static byte Compute(ReadOnlySpan<byte> data)
    {
        byte crc = 0x00;
        foreach (byte b in data)
        {
            crc ^= b;
            for (int j = 0; j < 8; j++)
                crc = (byte)((crc & 0x80) != 0 ? (crc << 1) ^ 0x07 : crc << 1);
        }
        return crc;
    }
}
```

- [ ] **Adım 4: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test --filter Crc8Tests`
Beklenen: PASS (4 test).

> Not: Adım 1'deki `0x9E` beklentisi elle hesaplanmıştır. Test kırmızı kalırsa önce
> `Compute("123456789") == 0xF4` testine bakın — o geçiyorsa algoritma doğrudur ve
> `0x9E` beklentisi düzeltilmelidir (gerçek değeri teste yazın).

- [ ] **Adım 5: Commit**

```bash
git add BmsUi/Protocol/Crc8.cs BmsUi.Tests/Crc8Tests.cs
git commit -m "feat: CRC-8/SMBUS hesabi"
```

---

### Görev 3: Protokol sabitleri ve fault çözümleme

**Dosyalar:**
- Oluştur: `BmsUi/Protocol/HvProtocol.cs`, `BmsUi.Tests/HvProtocolTests.cs`

**Arayüzler:**
- Üretir: `HvProtocol` (komut/uzunluk sabitleri, `IsShadowedRegister`), `Reg` (MAINBUFFER
  indeksleri), `FaultBits.Names` (15 elemanlı `string[]`), `FaultBits.Decode(ushort)`.

- [ ] **Adım 1: Başarısız testi yaz** — `BmsUi.Tests/HvProtocolTests.cs`

```csharp
using BmsUi.Protocol;
using Xunit;

public class HvProtocolTests
{
    [Fact]
    public void FaultNames_Has15Entries()
        => Assert.Equal(15, FaultBits.Names.Length);

    [Fact]
    public void Decode_NoBits_ReturnsEmpty()
        => Assert.Empty(FaultBits.Decode(0x0000));

    [Fact]
    public void Decode_Bit2AndBit13_ReturnsCellOvAndPrechargeTimeout()
    {
        var names = FaultBits.Decode((ushort)((1 << 2) | (1 << 13)));
        Assert.Equal(2, names.Count);
        Assert.Equal(FaultBits.Names[2], names[0]);
        Assert.Equal(FaultBits.Names[13], names[1]);
    }

    [Theory]
    [InlineData(0x29, true)]   // hucre voltaj komutu
    [InlineData(0x2A, true)]   // hucre sicaklik komutu
    [InlineData(0x2B, true)]   // balans komutu
    [InlineData(0x08, false)]  // PACK_CURRENT — normal register
    public void IsShadowedRegister_DetectsCommandCollisions(byte idx, bool expected)
        => Assert.Equal(expected, HvProtocol.IsShadowedRegister(idx));

    [Fact]
    public void FrameLengths_MatchFirmware()
    {
        Assert.Equal(194, HvProtocol.CellFrameLength);
        Assert.Equal(14, HvProtocol.BalanceFrameLength);
        Assert.Equal(4, HvProtocol.RegisterFrameLength);
        Assert.Equal(96, HvProtocol.CellCount);
    }
}
```

- [ ] **Adım 2: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter HvProtocolTests` → derleme hatası.

- [ ] **Adım 3: Uygulamayı yaz** — `BmsUi/Protocol/HvProtocol.cs`

```csharp
namespace BmsUi.Protocol;

/// <summary>HV BMS USB CDC protokol sabitleri (firmware main.cpp USB_Task ile birebir).</summary>
public static class HvProtocol
{
    public const byte CmdCellVoltages = 0x29;   // 41
    public const byte CmdCellTemps    = 0x2A;   // 42
    public const byte CmdBalance      = 0x2B;   // 43

    public static readonly byte[] PingCommand = { 0x17, 0x71 };
    public const int PingResponseLength = 2;

    public const int CellCount        = 96;
    public const int SegmentCount     = 6;
    public const int CellsPerSegment  = 16;

    public const int CellFrameLength     = 194;  // 96*2 + id + crc
    public const int BalanceFrameLength  = 14;   // 6*2 + id + crc
    public const int RegisterFrameLength = 4;    // 2 + id + crc

    public const byte MaxRegisterIndexExclusive = 50;  // firmware: idx < 50

    /// <summary>
    /// 41/42/43 indeksleri len=1 komutlarinda ozel komut olarak yakalandigi icin
    /// MAINBUFFER register'i olarak okunamaz (main.cpp:1960-1995).
    /// </summary>
    public static bool IsShadowedRegister(byte index)
        => index == CmdCellVoltages || index == CmdCellTemps || index == CmdBalance;

    public static bool IsValidRegister(byte index)
        => index < MaxRegisterIndexExclusive && !IsShadowedRegister(index);
}

/// <summary>MAINBUFFER indeksleri (firmware Core/Inc/main.h:93-123).</summary>
public static class Reg
{
    public const byte Faults              = 0;
    public const byte Outputs             = 1;
    public const byte PackVoltage         = 7;
    public const byte PackCurrent         = 8;   // isaretli, x10
    public const byte MaxCellVoltage      = 9;
    public const byte MinCellVoltage      = 10;
    public const byte TotalCellVoltage    = 11;
    public const byte MaxCellTemp         = 12;  // isaretli
    public const byte MinCellTemp         = 13;  // isaretli
    public const byte AvgCellVoltage      = 14;
    public const byte AvgCellTemp         = 15;
    public const byte MaxSlaveTemp        = 16;  // isaretli
    public const byte EstimatedSoc        = 17;  // x10000
    public const byte AllowedDisbalance   = 30;  // mV, yazilabilir
    public const byte PrechargePercentage = 32;  // yazilabilir
    public const byte PrechargeTimeout    = 33;  // yazilabilir
}

/// <summary>OUTPUTS (idx 1) bit maskeleri.</summary>
public static class OutputBits
{
    public const ushort Air = 1 << 0;
    public const ushort Pre = 1 << 1;
    public const ushort Err = 1 << 2;   // SDC / ERROR_OUT
}

/// <summary>FAULTS (idx 0) bit maskesi cozumlemesi.</summary>
public static class FaultBits
{
    public static readonly string[] Names =
    {
        "PEC / haberlesme hatasi",      // 0
        "Hucre dusuk voltaj",           // 1
        "Hucre asiri voltaj",           // 2
        "Desarj asiri akim",            // 3
        "Sarj asiri akim",              // 4
        "Hucre dusuk sicaklik",         // 5
        "Hucre asiri sicaklik",         // 6
        "Hucre kopuk kablo",            // 7
        "Akim sensoru yok",             // 8
        "Slave asiri sicaklik",         // 9
        "Paket dusuk voltaj",           // 10
        "Paket asiri voltaj",           // 11
        "Sicaklik kopuk kablo",         // 12
        "Precharge zaman asimi",        // 13
        "Olcum bayat (stale)",          // 14
    };

    public static IReadOnlyList<string> Decode(ushort mask)
    {
        var list = new List<string>();
        for (int i = 0; i < Names.Length; i++)
            if ((mask & (1 << i)) != 0) list.Add(Names[i]);
        return list;
    }

    public static bool IsSet(ushort mask, int bit) => (mask & (1 << bit)) != 0;
}
```

- [ ] **Adım 4: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test --filter HvProtocolTests` → PASS.

- [ ] **Adım 5: Commit**

```bash
git add BmsUi/Protocol/HvProtocol.cs BmsUi.Tests/HvProtocolTests.cs
git commit -m "feat: protokol sabitleri ve fault cozumleme"
```

---

### Görev 4: Çerçeve ayrıştırıcı (FrameParser)

**Dosyalar:**
- Oluştur: `BmsUi/Protocol/FrameParser.cs`, `BmsUi.Tests/FrameParserTests.cs`,
  `BmsUi.Tests/FrameBuilder.cs` (test yardımcısı)

**Arayüzler:**
- Tüketir: `Crc8.Compute`, `HvProtocol.*`
- Üretir:
  - `bool FrameParser.TryParseCellVoltages(ReadOnlySpan<byte> frame, double[] target, out string? error)`
  - `bool FrameParser.TryParseCellTemps(ReadOnlySpan<byte> frame, double[] target, out string? error)`
  - `bool FrameParser.TryParseBalance(ReadOnlySpan<byte> frame, ushort[] target, out string? error)`
  - `bool FrameParser.TryParseRegister(ReadOnlySpan<byte> frame, byte expectedIndex, out ushort value, out string? error)`

- [ ] **Adım 1: Test yardımcısını yaz** — `BmsUi.Tests/FrameBuilder.cs`

```csharp
using System.Buffers.Binary;
using BmsUi.Protocol;

/// <summary>Testlerde firmware'in urettigi cerceveleri taklit eder.</summary>
public static class FrameBuilder
{
    public static byte[] CellFrame(short[] rawValues, byte id)
    {
        var f = new byte[HvProtocol.CellFrameLength];
        for (int i = 0; i < HvProtocol.CellCount; i++)
            BinaryPrimitives.WriteInt16LittleEndian(f.AsSpan(i * 2, 2), rawValues[i]);
        f[192] = id;
        f[193] = Crc8.Compute(f.AsSpan(0, 193));
        return f;
    }

    public static byte[] BalanceFrame(ushort[] dcc)
    {
        var f = new byte[HvProtocol.BalanceFrameLength];
        for (int i = 0; i < 6; i++)
            BinaryPrimitives.WriteUInt16LittleEndian(f.AsSpan(i * 2, 2), dcc[i]);
        f[12] = HvProtocol.CmdBalance;
        f[13] = Crc8.Compute(f.AsSpan(0, 13));
        return f;
    }

    public static byte[] RegisterFrame(byte idx, ushort value)
    {
        var f = new byte[HvProtocol.RegisterFrameLength];
        BinaryPrimitives.WriteUInt16LittleEndian(f.AsSpan(0, 2), value);
        f[2] = idx;
        f[3] = Crc8.Compute(f.AsSpan(0, 3));
        return f;
    }
}
```

- [ ] **Adım 2: Başarısız testleri yaz** — `BmsUi.Tests/FrameParserTests.cs`

```csharp
using BmsUi.Protocol;
using Xunit;

public class FrameParserTests
{
    private static short[] Ramp(short start) 
    {
        var a = new short[96];
        for (int i = 0; i < 96; i++) a[i] = (short)(start + i);
        return a;
    }

    [Fact]
    public void TryParseCellVoltages_ValidFrame_ScalesByHundred()
    {
        var frame = FrameBuilder.CellFrame(Ramp(360), HvProtocol.CmdCellVoltages);
        var volts = new double[96];
        Assert.True(FrameParser.TryParseCellVoltages(frame, volts, out var err), err);
        Assert.Equal(3.60, volts[0], 3);
        Assert.Equal(4.55, volts[95], 3);   // 360+95 = 455 -> 4.55 V
    }

    [Fact]
    public void TryParseCellTemps_NegativeValues_StaySigned()
    {
        var raw = new short[96];
        raw[0] = -1250;    // -12.50 C
        raw[1] = 2537;     //  25.37 C
        raw[95] = -50;     //  -0.50 C
        var frame = FrameBuilder.CellFrame(raw, HvProtocol.CmdCellTemps);
        var temps = new double[96];
        Assert.True(FrameParser.TryParseCellTemps(frame, temps, out var err), err);
        Assert.Equal(-12.50, temps[0], 3);
        Assert.Equal(25.37, temps[1], 3);
        Assert.Equal(-0.50, temps[95], 3);
    }

    [Fact]
    public void TryParseCellVoltages_CorruptedCrc_Fails()
    {
        var frame = FrameBuilder.CellFrame(Ramp(360), HvProtocol.CmdCellVoltages);
        frame[193] ^= 0xFF;
        Assert.False(FrameParser.TryParseCellVoltages(frame, new double[96], out var err));
        Assert.Contains("CRC", err);
    }

    [Fact]
    public void TryParseCellVoltages_WrongId_Fails()
    {
        // Sicaklik cercevesi voltaj olarak ayristirilirsa reddedilmeli
        var frame = FrameBuilder.CellFrame(Ramp(360), HvProtocol.CmdCellTemps);
        Assert.False(FrameParser.TryParseCellVoltages(frame, new double[96], out var err));
        Assert.Contains("kimlik", err);
    }

    [Fact]
    public void TryParseCellVoltages_ShortFrame_Fails()
        => Assert.False(FrameParser.TryParseCellVoltages(new byte[193], new double[96], out _));

    [Fact]
    public void TryParseBalance_BitmapDecoded()
    {
        var dcc = new ushort[] { 0b0000_0000_0000_0101, 0, 0, 0, 0, 0b1000_0000_0000_0000 };
        var frame = FrameBuilder.BalanceFrame(dcc);
        var target = new ushort[6];
        Assert.True(FrameParser.TryParseBalance(frame, target, out var err), err);
        Assert.Equal(0b101, target[0]);
        Assert.Equal(0x8000, target[5]);
    }

    [Fact]
    public void TryParseRegister_MatchingIndex_ReturnsValue()
    {
        var frame = FrameBuilder.RegisterFrame(Reg.PackVoltage, 41234);
        Assert.True(FrameParser.TryParseRegister(frame, Reg.PackVoltage, out var v, out var err), err);
        Assert.Equal(41234, v);
    }

    [Fact]
    public void TryParseRegister_IndexMismatch_Fails()
    {
        var frame = FrameBuilder.RegisterFrame(Reg.PackVoltage, 100);
        Assert.False(FrameParser.TryParseRegister(frame, Reg.PackCurrent, out _, out var err));
        Assert.Contains("kimlik", err);
    }

    [Fact]
    public void TryParseRegister_NegativeCurrent_InterpretedBySigned()
    {
        // -37.5 A -> raw -375 -> uint16 olarak 65161
        var frame = FrameBuilder.RegisterFrame(Reg.PackCurrent, unchecked((ushort)-375));
        Assert.True(FrameParser.TryParseRegister(frame, Reg.PackCurrent, out var v, out _));
        Assert.Equal(-375, (short)v);
        Assert.Equal(-37.5, (short)v / 10.0, 3);
    }
}
```

- [ ] **Adım 3: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter FrameParserTests` → derleme hatası (`FrameParser` yok).

- [ ] **Adım 4: Uygulamayı yaz** — `BmsUi/Protocol/FrameParser.cs`

```csharp
using System.Buffers.Binary;

namespace BmsUi.Protocol;

/// <summary>Cihazdan gelen sabit uzunluklu binary cerceveleri dogrular ve cozer.</summary>
public static class FrameParser
{
    private static bool ValidateEnvelope(ReadOnlySpan<byte> frame, int expectedLength,
                                         byte expectedId, out string? error)
    {
        if (frame.Length != expectedLength)
        {
            error = $"Beklenen uzunluk {expectedLength}, gelen {frame.Length}";
            return false;
        }
        if (frame[expectedLength - 2] != expectedId)
        {
            error = $"Cerceve kimlik uyusmazligi: beklenen 0x{expectedId:X2}, " +
                    $"gelen 0x{frame[expectedLength - 2]:X2}";
            return false;
        }
        byte crc = Crc8.Compute(frame[..(expectedLength - 1)]);
        if (crc != frame[expectedLength - 1])
        {
            error = $"CRC uyusmazligi: hesaplanan 0x{crc:X2}, " +
                    $"gelen 0x{frame[expectedLength - 1]:X2}";
            return false;
        }
        error = null;
        return true;
    }

    public static bool TryParseCellVoltages(ReadOnlySpan<byte> frame, double[] target,
                                            out string? error)
    {
        if (!ValidateEnvelope(frame, HvProtocol.CellFrameLength,
                              HvProtocol.CmdCellVoltages, out error)) return false;
        for (int i = 0; i < HvProtocol.CellCount; i++)
            target[i] = BinaryPrimitives.ReadUInt16LittleEndian(frame.Slice(i * 2, 2)) / 100.0;
        return true;
    }

    public static bool TryParseCellTemps(ReadOnlySpan<byte> frame, double[] target,
                                         out string? error)
    {
        if (!ValidateEnvelope(frame, HvProtocol.CellFrameLength,
                              HvProtocol.CmdCellTemps, out error)) return false;
        // ISARETLI: int16 olarak okunur, aksi halde negatif sicakliklar 655.xx gorunur
        for (int i = 0; i < HvProtocol.CellCount; i++)
            target[i] = BinaryPrimitives.ReadInt16LittleEndian(frame.Slice(i * 2, 2)) / 100.0;
        return true;
    }

    public static bool TryParseBalance(ReadOnlySpan<byte> frame, ushort[] target,
                                       out string? error)
    {
        if (!ValidateEnvelope(frame, HvProtocol.BalanceFrameLength,
                              HvProtocol.CmdBalance, out error)) return false;
        for (int i = 0; i < HvProtocol.SegmentCount; i++)
            target[i] = BinaryPrimitives.ReadUInt16LittleEndian(frame.Slice(i * 2, 2));
        return true;
    }

    public static bool TryParseRegister(ReadOnlySpan<byte> frame, byte expectedIndex,
                                        out ushort value, out string? error)
    {
        value = 0;
        if (!ValidateEnvelope(frame, HvProtocol.RegisterFrameLength,
                              expectedIndex, out error)) return false;
        value = BinaryPrimitives.ReadUInt16LittleEndian(frame[..2]);
        return true;
    }
}
```

- [ ] **Adım 5: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test --filter FrameParserTests` → PASS (9 test).

- [ ] **Adım 6: Commit**

```bash
git add BmsUi/Protocol/FrameParser.cs BmsUi.Tests/FrameParserTests.cs BmsUi.Tests/FrameBuilder.cs
git commit -m "feat: binary cerceve ayristirici (CRC + kimlik dogrulamali)"
```

---

### Görev 5: Seri taşıma katmanı ve SerialLink

**Dosyalar:**
- Oluştur: `BmsUi/Serial/ISerialTransport.cs`, `BmsUi/Serial/SerialPortTransport.cs`,
  `BmsUi/Serial/SerialLink.cs`, `BmsUi.Tests/FakeTransport.cs`, `BmsUi.Tests/SerialLinkTests.cs`

**Arayüzler:**
- Tüketir: `FrameParser`, `HvProtocol`
- Üretir:
  - `interface ISerialTransport : IDisposable` — `bool IsOpen`, `void Open()`, `void Close()`,
    `void DiscardInBuffer()`, `void Write(byte[] buf, int offset, int count)`,
    `int Read(byte[] buf, int offset, int count)` (veri yoksa `TimeoutException` fırlatır)
  - `sealed class SerialLink` — `bool Ping()`, `byte[]? Transact(byte[] cmd, int expectedLength, byte expectedId)`,
    `ushort? ReadRegister(byte index)`, `ushort? WriteRegister(byte index, ushort value)`,
    `int CrcErrorCount`, `int TimeoutCount`, `int IdMismatchCount`, `int ConsecutiveFailures`

- [ ] **Adım 1: Sahte taşıma katmanını yaz** — `BmsUi.Tests/FakeTransport.cs`

```csharp
using BmsUi.Serial;

/// <summary>
/// Testler icin ISerialTransport: yazilan komutlari kaydeder, sirali cevaplar dondurur.
/// Cevaplar parca parca verilebilir (CDC'nin parcali teslimini taklit eder).
/// </summary>
public sealed class FakeTransport : ISerialTransport
{
    private readonly Queue<byte[]> _chunks = new();
    public List<byte[]> Written { get; } = new();
    public bool IsOpen { get; private set; } = true;
    public int DiscardCount { get; private set; }

    /// <summary>Cevabi tek parca halinde kuyruga koyar.</summary>
    public void EnqueueResponse(byte[] response) => _chunks.Enqueue(response);

    /// <summary>Cevabi verilen boyutlarda parcalara bolerek kuyruga koyar.</summary>
    public void EnqueueChunked(byte[] response, params int[] sizes)
    {
        int off = 0;
        foreach (int size in sizes)
        {
            _chunks.Enqueue(response[off..(off + size)]);
            off += size;
        }
        if (off < response.Length) _chunks.Enqueue(response[off..]);
    }

    public void Open() => IsOpen = true;
    public void Close() => IsOpen = false;
    public void DiscardInBuffer() { DiscardCount++; _chunks.Clear(); }
    public void Write(byte[] buf, int offset, int count)
        => Written.Add(buf[offset..(offset + count)]);

    public int Read(byte[] buf, int offset, int count)
    {
        if (_chunks.Count == 0) throw new TimeoutException("FakeTransport: veri yok");
        var chunk = _chunks.Dequeue();
        int n = Math.Min(chunk.Length, count);
        chunk.AsSpan(0, n).CopyTo(buf.AsSpan(offset, n));
        if (n < chunk.Length) _chunks.Enqueue(chunk[n..]);   // artani geri koy
        return n;
    }

    public void Dispose() { }
}
```

- [ ] **Adım 2: Başarısız testleri yaz** — `BmsUi.Tests/SerialLinkTests.cs`

```csharp
using BmsUi.Protocol;
using BmsUi.Serial;
using Xunit;

public class SerialLinkTests
{
    [Fact]
    public void Ping_EchoReceived_ReturnsTrue()
    {
        var t = new FakeTransport();
        t.EnqueueResponse(new byte[] { 0x17, 0x71 });
        using var link = new SerialLink(t);
        Assert.True(link.Ping());
        Assert.Equal(new byte[] { 0x17, 0x71 }, t.Written[0]);
    }

    [Fact]
    public void Ping_WrongEcho_ReturnsFalse()
    {
        var t = new FakeTransport();
        t.EnqueueResponse(new byte[] { 0x00, 0x00 });
        using var link = new SerialLink(t);
        Assert.False(link.Ping());
    }

    [Fact]
    public void Ping_NoResponse_ReturnsFalseAndCountsTimeout()
    {
        var t = new FakeTransport();
        using var link = new SerialLink(t);
        Assert.False(link.Ping());
        Assert.Equal(1, link.TimeoutCount);
    }

    [Fact]
    public void Transact_ChunkedResponse_Reassembles()
    {
        var raw = new short[96];
        for (int i = 0; i < 96; i++) raw[i] = 400;
        var frame = FrameBuilder.CellFrame(raw, HvProtocol.CmdCellVoltages);

        var t = new FakeTransport();
        t.EnqueueChunked(frame, 64, 64, 64, 2);   // CDC 64 baytlik paketler
        using var link = new SerialLink(t);

        var got = link.Transact(new[] { HvProtocol.CmdCellVoltages },
                                HvProtocol.CellFrameLength, HvProtocol.CmdCellVoltages);
        Assert.NotNull(got);
        Assert.Equal(frame, got);
    }

    [Fact]
    public void Transact_CorruptCrc_ReturnsNullAndCountsCrcError()
    {
        var frame = FrameBuilder.RegisterFrame(Reg.PackVoltage, 1234);
        frame[3] ^= 0xFF;
        var t = new FakeTransport();
        t.EnqueueResponse(frame);
        using var link = new SerialLink(t);

        Assert.Null(link.Transact(new[] { Reg.PackVoltage },
                                  HvProtocol.RegisterFrameLength, Reg.PackVoltage));
        Assert.Equal(1, link.CrcErrorCount);
    }

    [Fact]
    public void Transact_DiscardsInputBufferBeforeWriting()
    {
        var t = new FakeTransport();
        t.EnqueueResponse(FrameBuilder.RegisterFrame(Reg.Faults, 0));
        using var link = new SerialLink(t);
        link.ReadRegister(Reg.Faults);
        Assert.Equal(1, t.DiscardCount);
    }

    [Fact]
    public void ReadRegister_ReturnsValue_AndResetsConsecutiveFailures()
    {
        var t = new FakeTransport();
        t.EnqueueResponse(FrameBuilder.RegisterFrame(Reg.Faults, 0x0006));
        using var link = new SerialLink(t);
        Assert.Equal((ushort)0x0006, link.ReadRegister(Reg.Faults));
        Assert.Equal(0, link.ConsecutiveFailures);
    }

    [Fact]
    public void ReadRegister_ShadowedIndex_ThrowsWithoutTouchingPort()
    {
        var t = new FakeTransport();
        using var link = new SerialLink(t);
        Assert.Throws<ArgumentOutOfRangeException>(() => link.ReadRegister(0x29));
        Assert.Empty(t.Written);
    }

    [Fact]
    public void WriteRegister_SendsThreeBytesLittleEndian()
    {
        var t = new FakeTransport();
        t.EnqueueResponse(FrameBuilder.RegisterFrame(Reg.AllowedDisbalance, 0x0102));
        using var link = new SerialLink(t);

        Assert.Equal((ushort)0x0102, link.WriteRegister(Reg.AllowedDisbalance, 0x0102));
        Assert.Equal(new byte[] { Reg.AllowedDisbalance, 0x02, 0x01 }, t.Written[0]);
    }

    [Fact]
    public void ConsecutiveFailures_IncrementsOnRepeatedTimeouts()
    {
        var t = new FakeTransport();
        using var link = new SerialLink(t);
        link.ReadRegister(Reg.Faults);
        link.ReadRegister(Reg.Faults);
        Assert.Equal(2, link.ConsecutiveFailures);
    }
}
```

- [ ] **Adım 3: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter SerialLinkTests` → derleme hatası.

- [ ] **Adım 4: `ISerialTransport` ve `SerialPortTransport` yaz**

`BmsUi/Serial/ISerialTransport.cs`:

```csharp
namespace BmsUi.Serial;

/// <summary>
/// Seri I/O soyutlamasi. SerialLink'in gercek COM portu olmadan test edilebilmesi icin var.
/// Read(): veri gelmezse TimeoutException firlatir (SerialPort davranisiyla ayni).
/// </summary>
public interface ISerialTransport : IDisposable
{
    bool IsOpen { get; }
    void Open();
    void Close();
    void DiscardInBuffer();
    void Write(byte[] buffer, int offset, int count);
    int Read(byte[] buffer, int offset, int count);
}
```

`BmsUi/Serial/SerialPortTransport.cs`:

```csharp
using System.IO.Ports;

namespace BmsUi.Serial;

/// <summary>System.IO.Ports.SerialPort sarmalayicisi. ReadLine() KULLANILMAZ.</summary>
public sealed class SerialPortTransport : ISerialTransport
{
    private readonly SerialPort _port;

    public SerialPortTransport(string portName, int baudRate = 115200,
                               int readTimeoutMs = 200, int writeTimeoutMs = 200)
    {
        _port = new SerialPort(portName, baudRate, Parity.None, 8, StopBits.One)
        {
            ReadTimeout = readTimeoutMs,
            WriteTimeout = writeTimeoutMs,
            DtrEnable = true,       // CDC cihazlar genelde DTR bekler
            RtsEnable = true,
        };
    }

    public bool IsOpen => _port.IsOpen;
    public void Open() => _port.Open();
    public void Close() { if (_port.IsOpen) _port.Close(); }
    public void DiscardInBuffer() { if (_port.IsOpen) _port.DiscardInBuffer(); }
    public void Write(byte[] buffer, int offset, int count) => _port.Write(buffer, offset, count);
    public int Read(byte[] buffer, int offset, int count) => _port.Read(buffer, offset, count);
    public void Dispose() { Close(); _port.Dispose(); }
}
```

- [ ] **Adım 5: `SerialLink` yaz** — `BmsUi/Serial/SerialLink.cs`

```csharp
using System.Diagnostics;
using BmsUi.Protocol;

namespace BmsUi.Serial;

/// <summary>
/// Senkron komut-cevap katmani. TEK transaction kurali: komut tek Write ile yazilir,
/// cevabin tamami okunmadan yeni komut gonderilmez (firmware paket UZUNLUGUNA gore
/// ayristirir; birlesen iki komut yanlis yorumlanir).
/// </summary>
public sealed class SerialLink : IDisposable
{
    private readonly ISerialTransport _transport;
    private readonly TimeSpan _deadline;
    private readonly byte[] _rx = new byte[HvProtocol.CellFrameLength];

    public SerialLink(ISerialTransport transport, TimeSpan? deadline = null)
    {
        _transport = transport;
        _deadline = deadline ?? TimeSpan.FromMilliseconds(300);
    }

    public int CrcErrorCount { get; private set; }
    public int TimeoutCount { get; private set; }
    public int IdMismatchCount { get; private set; }
    public int ConsecutiveFailures { get; private set; }
    public string? LastError { get; private set; }

    public bool IsOpen => _transport.IsOpen;
    public void Open() => _transport.Open();
    public void Close() => _transport.Close();

    /// <summary>0x17 0x71 gonderir, ayni iki baytin echo'sunu bekler (CRC yok).</summary>
    public bool Ping()
    {
        var echo = Exchange(HvProtocol.PingCommand, HvProtocol.PingResponseLength);
        if (echo is null) return false;
        bool ok = echo[0] == HvProtocol.PingCommand[0] && echo[1] == HvProtocol.PingCommand[1];
        if (!ok) { LastError = "Ping echo uyusmadi"; Fail(); }
        return ok;
    }

    /// <summary>Komutu yazar, tam uzunlukta cevabi okur, kimlik + CRC dogrular.</summary>
    public byte[]? Transact(byte[] command, int expectedLength, byte expectedId)
    {
        var frame = Exchange(command, expectedLength);
        if (frame is null) return null;

        if (frame[expectedLength - 2] != expectedId)
        {
            IdMismatchCount++;
            LastError = $"Kimlik uyusmazligi: beklenen 0x{expectedId:X2}, " +
                        $"gelen 0x{frame[expectedLength - 2]:X2}";
            Fail();
            return null;
        }
        if (Crc8.Compute(frame.AsSpan(0, expectedLength - 1)) != frame[expectedLength - 1])
        {
            CrcErrorCount++;
            LastError = "CRC uyusmazligi";
            Fail();
            return null;
        }
        ConsecutiveFailures = 0;
        return frame;
    }

    public ushort? ReadRegister(byte index)
    {
        if (!HvProtocol.IsValidRegister(index))
            throw new ArgumentOutOfRangeException(nameof(index),
                $"idx {index} okunamaz (>=50 veya 0x29/0x2A/0x2B ile golgeli)");

        var frame = Transact(new[] { index }, HvProtocol.RegisterFrameLength, index);
        if (frame is null) return null;
        return (ushort)(frame[0] | (frame[1] << 8));
    }

    public ushort? WriteRegister(byte index, ushort value)
    {
        if (!HvProtocol.IsValidRegister(index))
            throw new ArgumentOutOfRangeException(nameof(index));

        var cmd = new[] { index, (byte)(value & 0xFF), (byte)(value >> 8) };
        var frame = Transact(cmd, HvProtocol.RegisterFrameLength, index);
        if (frame is null) return null;
        return (ushort)(frame[0] | (frame[1] << 8));
    }

    /// <summary>Yaz + tam N bayta kadar biriktir. Zarf dogrulamasi yapmaz.</summary>
    private byte[]? Exchange(byte[] command, int expectedLength)
    {
        try
        {
            _transport.DiscardInBuffer();          // onceki timeout artiklari akisi zehirler
            _transport.Write(command, 0, command.Length);

            int got = 0;
            var sw = Stopwatch.StartNew();
            while (got < expectedLength)
            {
                int n = _transport.Read(_rx, got, expectedLength - got);
                if (n <= 0) break;
                got += n;
                if (sw.Elapsed > _deadline) break;
            }

            if (got < expectedLength)
            {
                TimeoutCount++;
                LastError = $"Eksik cevap: {got}/{expectedLength} bayt";
                Fail();
                return null;
            }
            return _rx[..expectedLength];
        }
        catch (TimeoutException)
        {
            TimeoutCount++;
            LastError = "Zaman asimi";
            Fail();
            return null;
        }
        catch (Exception ex)
        {
            LastError = ex.Message;
            Fail();
            return null;
        }
    }

    private void Fail() => ConsecutiveFailures++;

    public void ResetCounters()
        => CrcErrorCount = TimeoutCount = IdMismatchCount = ConsecutiveFailures = 0;

    public void Dispose() => _transport.Dispose();
}
```

- [ ] **Adım 6: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test --filter SerialLinkTests` → PASS (10 test).

- [ ] **Adım 7: Commit**

```bash
git add BmsUi/Serial BmsUi.Tests/FakeTransport.cs BmsUi.Tests/SerialLinkTests.cs
git commit -m "feat: seri tasima katmani ve komut-cevap SerialLink"
```

---

### Görev 6: Simülatör (bms_simulator.py)

**Dosyalar:**
- Oluştur: `bms_simulator.py`

**Arayüzler:**
- Üretir: firmware'i taklit eden bir seri cihaz. `python bms_simulator.py --port COM11`

- [ ] **Adım 1: Simülatörü yaz** — `bms_simulator.py`

```python
"""HV BMS USB CDC simulatoru.

Sanal seri port ciftinde (VSPE / com0com, orn. COM10 <-> COM11) calisir:
UI COM10'a baglanir, bu betik COM11'i tutar.

Firmware davranisi taklit edilir: gelen bayt obeginin UZUNLUGUNA gore komut ayristirilir
(main.cpp USB_Task -> switch (usblen)).
"""
import argparse
import math
import random
import struct
import sys
import time

import serial

CMD_VOLTAGES = 0x29
CMD_TEMPS = 0x2A
CMD_BALANCE = 0x2B
PING = bytes([0x17, 0x71])

CELL_COUNT = 96
SEGMENTS = 6
CELLS_PER_SEGMENT = 16


def crc8(data: bytes) -> int:
    """CRC-8/SMBUS — firmware calculateCRC8() ile birebir."""
    crc = 0x00
    for b in data:
        crc ^= b
        for _ in range(8):
            crc = ((crc << 1) ^ 0x07) & 0xFF if crc & 0x80 else (crc << 1) & 0xFF
    return crc


class PackState:
    """Gercekci suruklenme ureten basit paket modeli."""

    def __init__(self, fault_bits=0):
        self.voltages = [random.uniform(3.85, 3.95) for _ in range(CELL_COUNT)]
        self.temps = [random.uniform(24.0, 30.0) for _ in range(CELL_COUNT)]
        # birkac sicak hucre
        for i in (7, 42, 83):
            self.temps[i] += random.uniform(12.0, 18.0)
        self.current = 0.0
        self.t0 = time.time()
        self.fault_bits = fault_bits
        self.registers = [0] * 50

    def tick(self):
        t = time.time() - self.t0
        # Akim: yavas salinim (-120 A .. +80 A), sarj/desarj
        self.current = 80.0 * math.sin(t / 7.0) - 40.0 * math.sin(t / 3.0)
        for i in range(CELL_COUNT):
            # ic direnc dususu + gurultu
            sag = self.current * 0.00012
            self.voltages[i] += random.uniform(-0.0015, 0.0015) - sag * 0.01
            self.voltages[i] = min(4.19, max(3.30, self.voltages[i]))
            self.temps[i] += random.uniform(-0.05, 0.05) + abs(self.current) * 0.00008
            self.temps[i] = min(78.0, max(15.0, self.temps[i]))

        avg_v = sum(self.voltages) / CELL_COUNT
        pack_v = sum(self.voltages)
        outputs = 0
        if t > 2.0:
            outputs |= 1 << 1          # PRE
        if t > 5.0:
            outputs |= 1 << 0          # AIR
        if self.fault_bits:
            outputs |= 1 << 2          # ERR / SDC

        r = self.registers
        r[0] = self.fault_bits
        r[1] = outputs
        r[7] = int(pack_v * 100) & 0xFFFF
        r[8] = struct.unpack("<H", struct.pack("<h", int(self.current * 10)))[0]
        r[9] = int(max(self.voltages) * 100)
        r[10] = int(min(self.voltages) * 100)
        r[11] = int(pack_v * 100) & 0xFFFF
        r[12] = struct.unpack("<H", struct.pack("<h", int(max(self.temps) * 100)))[0]
        r[13] = struct.unpack("<H", struct.pack("<h", int(min(self.temps) * 100)))[0]
        r[14] = int(avg_v * 100)
        r[15] = struct.unpack("<H", struct.pack("<h",
                 int(sum(self.temps) / CELL_COUNT * 100)))[0]
        r[16] = struct.unpack("<H", struct.pack("<h", int(52.0 * 100)))[0]
        r[17] = int(min(1.0, max(0.0, (avg_v - 3.2) / (4.15 - 3.2))) * 10000)
        if r[30] == 0:
            r[30] = 20       # ALLOWED_DISBALANCE, mV
            r[32] = 95       # PRECHARGE_PERCENTAGE
            r[33] = 5000     # PRECHARGE_TIMEOUT

    def balance_bitmaps(self):
        """Ortalamanin ALLOWED_DISBALANCE kadar ustundeki hucreler balansta."""
        avg = sum(self.voltages) / CELL_COUNT
        threshold = avg + max(1, self.registers[30]) / 1000.0
        maps = []
        for ic in range(SEGMENTS):
            dcc = 0
            for c in range(CELLS_PER_SEGMENT):
                if self.voltages[ic * CELLS_PER_SEGMENT + c] > threshold:
                    dcc |= 1 << c
            maps.append(dcc)
        return maps


def cell_frame(values, scale, cmd_id, signed):
    fmt = "<h" if signed else "<H"
    payload = b"".join(struct.pack(fmt, int(v * scale)) for v in values)
    frame = bytearray(payload)
    frame.append(cmd_id)
    frame.append(crc8(bytes(frame)))
    return bytes(frame)


def balance_frame(bitmaps):
    frame = bytearray(b"".join(struct.pack("<H", m) for m in bitmaps))
    frame.append(CMD_BALANCE)
    frame.append(crc8(bytes(frame)))
    return bytes(frame)


def register_frame(idx, value):
    frame = bytearray(struct.pack("<H", value & 0xFFFF))
    frame.append(idx)
    frame.append(crc8(bytes(frame)))
    return bytes(frame)


def send(ser, data, chunked, latency_ms):
    if latency_ms:
        time.sleep(latency_ms / 1000.0)
    if chunked and len(data) > 8:
        # CDC'nin parcali teslimini taklit et
        for off in range(0, len(data), 64):
            ser.write(data[off:off + 64])
            ser.flush()
            time.sleep(0.001)
    else:
        ser.write(data)
        ser.flush()


def main():
    ap = argparse.ArgumentParser(description="HV BMS USB simulatoru")
    ap.add_argument("--port", default="COM11")
    ap.add_argument("--baud", type=int, default=115200)
    ap.add_argument("--fault", type=int, action="append", default=[],
                    help="FAULTS bit numarasi (birden fazla kez verilebilir)")
    ap.add_argument("--chunked", action="store_true",
                    help="cevaplari 64 baytlik parcalara bolerek gonder")
    ap.add_argument("--latency", type=int, default=0, help="cevap gecikmesi (ms)")
    ap.add_argument("--verbose", action="store_true")
    args = ap.parse_args()

    fault_mask = 0
    for b in args.fault:
        fault_mask |= 1 << b

    print("=" * 52)
    print("        HIGH VOLTAGE BMS USB SIMULATOR")
    print("=" * 52)
    print(f"Port: {args.port} | Baud: {args.baud} | Fault maskesi: 0x{fault_mask:04X}")

    try:
        ser = serial.Serial(args.port, args.baud, timeout=0.05)
    except Exception as e:
        print(f"HATA: {args.port} acilamadi: {e}")
        print("VSPE/com0com ile sanal port cifti olusturdugunuzdan emin olun.")
        return 1

    state = PackState(fault_mask)
    print("UI baglantisi bekleniyor... (Ctrl+C ile durdurun)")

    try:
        while True:
            first = ser.read(1)
            if not first:
                state.tick()
                continue
            # Firmware bir USB paketini tek parca gorur; burst'u topla
            time.sleep(0.002)
            extra = ser.read(ser.in_waiting or 0)
            packet = first + extra
            state.tick()

            if len(packet) == 1:
                cmd = packet[0]
                if cmd == CMD_VOLTAGES:
                    send(ser, cell_frame(state.voltages, 100, CMD_VOLTAGES, False),
                         args.chunked, args.latency)
                elif cmd == CMD_TEMPS:
                    send(ser, cell_frame(state.temps, 100, CMD_TEMPS, True),
                         args.chunked, args.latency)
                elif cmd == CMD_BALANCE:
                    send(ser, balance_frame(state.balance_bitmaps()),
                         args.chunked, args.latency)
                elif cmd < 50:
                    send(ser, register_frame(cmd, state.registers[cmd]),
                         args.chunked, args.latency)
                else:
                    if args.verbose:
                        print(f"idx {cmd} >= 50 — firmware gibi sessizce dusuruldu")
            elif len(packet) == 2:
                if packet == PING:
                    send(ser, PING, False, args.latency)
                    print("Ping alindi -> echo gonderildi")
            elif len(packet) == 3:
                idx, lsb, msb = packet[0], packet[1], packet[2]
                if idx < 50:
                    state.registers[idx] = (msb << 8) | lsb
                    send(ser, register_frame(idx, state.registers[idx]),
                         args.chunked, args.latency)
                    print(f"YAZ MAINBUFFER[{idx}] = {state.registers[idx]}")
            if args.verbose:
                print(f"paket={packet.hex(' ')} len={len(packet)}")
    except KeyboardInterrupt:
        print("\nSimulator durduruldu.")
    finally:
        ser.close()
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Adım 2: Simülatörün CRC'sini C# tarafıyla çapraz doğrula**

Çalıştır:

```bash
python -c "import bms_simulator as s; print(hex(s.crc8(b'123456789')))"
```

Beklenen: `0xf4` — Görev 2'deki C# testiyle aynı değer.

- [ ] **Adım 3: Commit**

```bash
git add bms_simulator.py
git commit -m "feat: HV BMS USB simulatoru (uzunluga gore komut ayristirma)"
```

---

### Görev 7: Bağlantı UI'ı ve uçtan uca ping (F1 tamamlanır)

**Dosyalar:**
- Değiştir: `BmsUi/Form1.cs`, `BmsUi/Form1.Designer.cs`

**Arayüzler:**
- Tüketir: `SerialPortTransport`, `SerialLink`
- Üretir: `Form1` üzerinde `_link` alanı ve bağlan/kes akışı; sonraki görevler bunun
  üstüne panel ekler.

- [ ] **Adım 1: Designer'da bağlantı çubuğunu kur**

`Form1.Designer.cs` içinde, üstte `Dock = Top` bir `Panel` (`connectionPanel`, yükseklik 40)
ve içine soldan sağa: `Label` ("Port:"), `ComboBox portCombo` (`DropDownList`, genişlik 120),
`Button refreshButton` ("Yenile"), `Button startButton` ("Start"), `Label statusLabel`
(bağlantı durumu). Form başlığı: `HV BMS Arayuzu`, `MinimumSize = 1100×700`.

- [ ] **Adım 2: Form1 bağlantı mantığını yaz**

```csharp
using BmsUi.Serial;

namespace BmsUi;

public partial class Form1 : Form
{
    private SerialLink? _link;

    public Form1()
    {
        InitializeComponent();
        RefreshPorts();
    }

    private void RefreshPorts()
    {
        portCombo.Items.Clear();
        portCombo.Items.AddRange(System.IO.Ports.SerialPort.GetPortNames());
        if (portCombo.Items.Count > 0) portCombo.SelectedIndex = 0;
    }

    private void refreshButton_Click(object sender, EventArgs e) => RefreshPorts();

    private void startButton_Click(object sender, EventArgs e)
    {
        if (_link is not null) { Disconnect(); return; }

        if (portCombo.SelectedItem is not string portName)
        {
            MessageBox.Show("Once bir COM portu secin.", "Uyari");
            return;
        }

        var transport = new SerialPortTransport(portName);
        var link = new SerialLink(transport);
        try
        {
            link.Open();
        }
        catch (Exception ex)
        {
            link.Dispose();
            MessageBox.Show($"{portName} acilamadi: {ex.Message}\n\n" +
                            "Port takili mi ve baska bir uygulama tarafindan kullaniliyor mu?",
                            "Hata");
            return;
        }

        // Bagli cihazin gercekten HV BMS oldugunu ping ile dogrula
        if (!link.Ping())
        {
            link.Dispose();
            MessageBox.Show($"{portName} acildi ama cihaz ping'e cevap vermedi " +
                            $"({link.LastError}).\n\nDogru port mu? Kart calisiyor mu?",
                            "Cihaz bulunamadi");
            return;
        }

        _link = link;
        startButton.Text = "Stop";
        portCombo.Enabled = refreshButton.Enabled = false;
        statusLabel.Text = $"Bagli: {portName}";
        statusLabel.ForeColor = Color.Green;
    }

    private void Disconnect()
    {
        _link?.Dispose();
        _link = null;
        startButton.Text = "Start";
        portCombo.Enabled = refreshButton.Enabled = true;
        statusLabel.Text = "Bagli degil";
        statusLabel.ForeColor = Color.DimGray;
    }

    protected override void OnFormClosing(FormClosingEventArgs e)
    {
        Disconnect();
        base.OnFormClosing(e);
    }
}
```

- [ ] **Adım 3: Derle**

Çalıştır: `dotnet build -c Release` → hatasız.

- [ ] **Adım 4: Uçtan uca elle test (simülatörle)**

1. VSPE veya com0com ile `COM10 ↔ COM11` çifti oluşturun.
2. `python bms_simulator.py --port COM11`
3. `dotnet run --project BmsUi` → COM10 seçip **Start**.

Beklenen: UI'da "Bagli: COM10" yeşil yazar; simülatör konsolunda
`Ping alindi -> echo gonderildi` görünür. Yanlış port seçilirse "cihaz ping'e cevap vermedi"
uyarısı çıkar.

- [ ] **Adım 5: Commit**

```bash
git add BmsUi/Form1.cs BmsUi/Form1.Designer.cs
git commit -m "feat(F1): COM port secimi, Start/Stop ve ping dogrulamali baglanti"
```

---

### Görev 8: Model — BmsSnapshot ve istatistikler

**Dosyalar:**
- Oluştur: `BmsUi/Model/CellStats.cs`, `BmsUi/Model/BmsSnapshot.cs`,
  `BmsUi.Tests/BmsSnapshotTests.cs`

**Arayüzler:**
- Üretir:
  - `readonly record struct CellStat(double Min, int MinIndex, double Max, int MaxIndex, double Avg, int ValidCount)`
  - `static CellStat CellStats.Voltage(double[] v)` — geçerlilik: `v >= 0.5`
  - `static CellStat CellStats.Temperature(double[] t)` — geçerlilik: `-50 < t < 150`
  - `sealed class BmsSnapshot` — `CellVoltages`, `CellTemps` (`double[96]`),
    `BalanceBitmaps` (`ushort[6]`), `Registers` (`ushort[50]`), `RegisterValid` (`bool[50]`),
    zaman damgaları, türetilmiş `PackVoltage/PackCurrent/PowerKw/SocPercent`,
    `VoltageStats`, `TempStats`, `bool IsBalancing(int cell)`, `ushort Faults`, `ushort Outputs`

- [ ] **Adım 1: Başarısız testleri yaz** — `BmsUi.Tests/BmsSnapshotTests.cs`

```csharp
using BmsUi.Model;
using BmsUi.Protocol;
using Xunit;

public class BmsSnapshotTests
{
    private static BmsSnapshot Make(Action<BmsSnapshot> tweak)
    {
        var s = BmsSnapshot.Empty();
        tweak(s);
        return s;
    }

    [Fact]
    public void VoltageStats_IgnoresZeroCells()
    {
        var v = new double[96];
        for (int i = 0; i < 96; i++) v[i] = 3.90;
        v[10] = 0.00;          // gecersiz / stale
        v[20] = 4.15;          // max
        v[30] = 3.55;          // min
        var stat = CellStats.Voltage(v);

        Assert.Equal(4.15, stat.Max, 3);
        Assert.Equal(20, stat.MaxIndex);
        Assert.Equal(3.55, stat.Min, 3);
        Assert.Equal(30, stat.MinIndex);
        Assert.Equal(95, stat.ValidCount);
    }

    [Fact]
    public void TemperatureStats_HandlesNegativeValues()
    {
        var t = new double[96];
        for (int i = 0; i < 96; i++) t[i] = 25.0;
        t[5] = -8.5;
        var stat = CellStats.Temperature(t);
        Assert.Equal(-8.5, stat.Min, 3);
        Assert.Equal(5, stat.MinIndex);
    }

    [Fact]
    public void PackCurrent_NegativeRegister_IsSigned()
    {
        var s = Make(x => x.SetRegister(Reg.PackCurrent, unchecked((ushort)-375)));
        Assert.Equal(-37.5, s.PackCurrent, 3);
    }

    [Fact]
    public void PowerKw_IsVoltageTimesCurrent()
    {
        var s = Make(x =>
        {
            x.SetRegister(Reg.PackVoltage, 40000);                       // 400.00 V
            x.SetRegister(Reg.PackCurrent, unchecked((ushort)1000));     // 100.0 A
        });
        Assert.Equal(400.0, s.PackVoltage, 3);
        Assert.Equal(100.0, s.PackCurrent, 3);
        Assert.Equal(40.0, s.PowerKw, 3);
    }

    [Fact]
    public void SocPercent_ScaledBy10000()
    {
        var s = Make(x => x.SetRegister(Reg.EstimatedSoc, 8250));
        Assert.Equal(82.50, s.SocPercent, 2);
    }

    [Fact]
    public void IsBalancing_ReadsBitFromSegmentBitmap()
    {
        var s = Make(x => x.SetBalance(new ushort[] { 0b0000_0000_0000_1000, 0, 0, 0, 0, 0 }));
        Assert.True(s.IsBalancing(3));       // segment 0, hucre 3
        Assert.False(s.IsBalancing(4));
    }

    [Fact]
    public void IsBalancing_LastSegmentLastCell()
    {
        var maps = new ushort[6];
        maps[5] = 1 << 15;
        var s = Make(x => x.SetBalance(maps));
        Assert.True(s.IsBalancing(95));      // 5*16 + 15
    }

    [Fact]
    public void Clone_ProducesIndependentCopy()
    {
        var s = Make(x => x.SetRegister(Reg.Faults, 0x0004));
        var c = s.Clone();
        c.SetRegister(Reg.Faults, 0x0000);
        Assert.Equal(0x0004, s.Faults);
        Assert.Equal(0x0000, c.Faults);
    }
}
```

- [ ] **Adım 2: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter BmsSnapshotTests` → derleme hatası.

- [ ] **Adım 3: `CellStats` yaz** — `BmsUi/Model/CellStats.cs`

```csharp
namespace BmsUi.Model;

public readonly record struct CellStat(
    double Min, int MinIndex, double Max, int MaxIndex, double Avg, int ValidCount)
{
    public static readonly CellStat None = new(0, -1, 0, -1, 0, 0);
    public bool HasData => ValidCount > 0;
}

public static class CellStats
{
    /// <summary>Voltaj: 0.5 V altindaki hucreler eksik/stale kabul edilip disarida birakilir.</summary>
    public static CellStat Voltage(double[] values) => Compute(values, v => v >= 0.5);

    /// <summary>Sicaklik: -50..150 C disi degerler sensor hatasi kabul edilir.</summary>
    public static CellStat Temperature(double[] values) => Compute(values, t => t > -50 && t < 150);

    private static CellStat Compute(double[] values, Func<double, bool> isValid)
    {
        double min = double.MaxValue, max = double.MinValue, sum = 0;
        int minIdx = -1, maxIdx = -1, count = 0;

        for (int i = 0; i < values.Length; i++)
        {
            double v = values[i];
            if (!isValid(v)) continue;
            if (v < min) { min = v; minIdx = i; }
            if (v > max) { max = v; maxIdx = i; }
            sum += v;
            count++;
        }
        return count == 0 ? CellStat.None
                          : new CellStat(min, minIdx, max, maxIdx, sum / count, count);
    }
}
```

- [ ] **Adım 4: `BmsSnapshot` yaz** — `BmsUi/Model/BmsSnapshot.cs`

```csharp
using BmsUi.Protocol;

namespace BmsUi.Model;

/// <summary>
/// Paketin tam durumu. PollWorker her turda doldurup UI'ya kopyasini gonderir;
/// o turda yenilenmeyen alanlar onceki degerleriyle tasinir, zaman damgalari veri
/// yasini gosterir.
/// </summary>
public sealed class BmsSnapshot
{
    public double[] CellVoltages { get; private set; } = new double[HvProtocol.CellCount];
    public double[] CellTemps    { get; private set; } = new double[HvProtocol.CellCount];
    public ushort[] BalanceBitmaps { get; private set; } = new ushort[HvProtocol.SegmentCount];
    public ushort[] Registers    { get; private set; } = new ushort[HvProtocol.MaxRegisterIndexExclusive];
    public bool[] RegisterValid  { get; private set; } = new bool[HvProtocol.MaxRegisterIndexExclusive];

    public DateTime VoltagesAt  { get; private set; }
    public DateTime TempsAt     { get; private set; }
    public DateTime BalanceAt   { get; private set; }
    public DateTime RegistersAt { get; private set; }

    public static BmsSnapshot Empty() => new();

    public void SetVoltages(double[] v) { Array.Copy(v, CellVoltages, v.Length); VoltagesAt = DateTime.Now; }
    public void SetTemps(double[] t)    { Array.Copy(t, CellTemps, t.Length);    TempsAt = DateTime.Now; }
    public void SetBalance(ushort[] b)  { Array.Copy(b, BalanceBitmaps, b.Length); BalanceAt = DateTime.Now; }

    public void SetRegister(byte idx, ushort value)
    {
        Registers[idx] = value;
        RegisterValid[idx] = true;
        RegistersAt = DateTime.Now;
    }

    public ushort Faults  => Registers[Reg.Faults];
    public ushort Outputs => Registers[Reg.Outputs];
    public bool AirClosed => (Outputs & OutputBits.Air) != 0;
    public bool PreActive => (Outputs & OutputBits.Pre) != 0;
    public bool ErrActive => (Outputs & OutputBits.Err) != 0;

    public double PackVoltage => Registers[Reg.PackVoltage] / 100.0;
    public double PackCurrent => (short)Registers[Reg.PackCurrent] / 10.0;
    public double PowerKw     => PackVoltage * PackCurrent / 1000.0;
    public double SocPercent  => Registers[Reg.EstimatedSoc] / 10000.0 * 100.0;
    public double TotalCellVoltage => Registers[Reg.TotalCellVoltage] / 100.0;
    public double MaxSlaveTemp     => (short)Registers[Reg.MaxSlaveTemp] / 100.0;

    // Firmware'in bildirdigi ozetler (host hesabiyla capraz kontrol icin)
    public double FwMaxCellVoltage => Registers[Reg.MaxCellVoltage] / 100.0;
    public double FwMinCellVoltage => Registers[Reg.MinCellVoltage] / 100.0;
    public double FwAvgCellVoltage => Registers[Reg.AvgCellVoltage] / 100.0;
    public double FwMaxCellTemp    => (short)Registers[Reg.MaxCellTemp] / 100.0;
    public double FwMinCellTemp    => (short)Registers[Reg.MinCellTemp] / 100.0;
    public double FwAvgCellTemp    => (short)Registers[Reg.AvgCellTemp] / 100.0;

    public CellStat VoltageStats => CellStats.Voltage(CellVoltages);
    public CellStat TempStats    => CellStats.Temperature(CellTemps);

    /// <summary>cell: 0..95 lineer indeks (segment*16 + hucre).</summary>
    public bool IsBalancing(int cell)
    {
        int seg = cell / HvProtocol.CellsPerSegment;
        int bit = cell % HvProtocol.CellsPerSegment;
        return (BalanceBitmaps[seg] & (1 << bit)) != 0;
    }

    public BmsSnapshot Clone() => new()
    {
        CellVoltages = (double[])CellVoltages.Clone(),
        CellTemps = (double[])CellTemps.Clone(),
        BalanceBitmaps = (ushort[])BalanceBitmaps.Clone(),
        Registers = (ushort[])Registers.Clone(),
        RegisterValid = (bool[])RegisterValid.Clone(),
        VoltagesAt = VoltagesAt, TempsAt = TempsAt,
        BalanceAt = BalanceAt, RegistersAt = RegistersAt,
    };
}
```

- [ ] **Adım 5: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test --filter BmsSnapshotTests` → PASS (8 test).

- [ ] **Adım 6: Commit**

```bash
git add BmsUi/Model BmsUi.Tests/BmsSnapshotTests.cs
git commit -m "feat: BmsSnapshot modeli ve hucre istatistikleri"
```

---

### Görev 9: Poll zamanlaması ve worker thread

**Dosyalar:**
- Oluştur: `BmsUi/Polling/PollSchedule.cs`, `BmsUi/Polling/PollWorker.cs`,
  `BmsUi.Tests/PollScheduleTests.cs`

**Arayüzler:**
- Tüketir: `SerialLink`, `FrameParser`, `BmsSnapshot`
- Üretir:
  - `enum PollItem { FastRegisters, SummaryRegisters, CellVoltages, CellTemps, Balance }`
  - `static IReadOnlyList<PollItem> PollSchedule.ItemsForTick(long tick)`
  - `static readonly byte[] PollSchedule.FastRegisters` = `{0, 1, 7, 8}`
  - `static readonly byte[] PollSchedule.SummaryRegisters` = `{9,10,11,12,13,14,15,16,17}`
  - `sealed class PollWorker` — `event Action<BmsSnapshot>? SnapshotReady`,
    `event Action<string>? ConnectionLost`, `void Start()`, `void Stop()`,
    `void EnqueueWrite(byte index, ushort value, Action<ushort?> callback)`

- [ ] **Adım 1: Başarısız testleri yaz** — `BmsUi.Tests/PollScheduleTests.cs`

```csharp
using BmsUi.Polling;
using Xunit;

public class PollScheduleTests
{
    [Fact]
    public void Tick0_IncludesEverything()
    {
        var items = PollSchedule.ItemsForTick(0);
        Assert.Contains(PollItem.FastRegisters, items);
        Assert.Contains(PollItem.SummaryRegisters, items);
        Assert.Contains(PollItem.CellVoltages, items);
        Assert.Contains(PollItem.CellTemps, items);
        Assert.Contains(PollItem.Balance, items);
    }

    [Fact]
    public void OddTick_OnlyFastRegisters()
    {
        var items = PollSchedule.ItemsForTick(1);
        Assert.Equal(new[] { PollItem.FastRegisters }, items);
    }

    [Fact]
    public void EvenTick_AddsCellsAndSummary_ButNotBalance()
    {
        var items = PollSchedule.ItemsForTick(2);   // 5 Hz
        Assert.Contains(PollItem.CellVoltages, items);
        Assert.Contains(PollItem.CellTemps, items);
        Assert.Contains(PollItem.SummaryRegisters, items);
        Assert.DoesNotContain(PollItem.Balance, items);
    }

    [Fact]
    public void BalanceEveryFifthTick_Gives2Hz()
    {
        Assert.Contains(PollItem.Balance, PollSchedule.ItemsForTick(5));
        Assert.Contains(PollItem.Balance, PollSchedule.ItemsForTick(10));
        Assert.DoesNotContain(PollItem.Balance, PollSchedule.ItemsForTick(6));
    }

    [Fact]
    public void RegisterLists_ContainNoShadowedIndices()
    {
        foreach (byte idx in PollSchedule.FastRegisters.Concat(PollSchedule.SummaryRegisters))
            Assert.True(BmsUi.Protocol.HvProtocol.IsValidRegister(idx), $"idx {idx} gecersiz");
    }

    [Fact]
    public void TransactionsPerSecond_MatchesDesignBudget()
    {
        // 1 saniye = 10 tick; tasarim butcesi ~97 transaction/s
        int total = 0;
        for (long t = 0; t < 10; t++)
            foreach (var item in PollSchedule.ItemsForTick(t))
                total += item switch
                {
                    PollItem.FastRegisters => PollSchedule.FastRegisters.Length,
                    PollItem.SummaryRegisters => PollSchedule.SummaryRegisters.Length,
                    _ => 1,
                };
        Assert.InRange(total, 90, 105);
    }
}
```

- [ ] **Adım 2: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter PollScheduleTests` → derleme hatası.

- [ ] **Adım 3: `PollSchedule` yaz** — `BmsUi/Polling/PollSchedule.cs`

```csharp
using BmsUi.Protocol;

namespace BmsUi.Polling;

public enum PollItem { FastRegisters, SummaryRegisters, CellVoltages, CellTemps, Balance }

/// <summary>
/// 10 Hz temel tick uzerinde katmanli zamanlama (saf fonksiyon — test edilebilir):
/// her tick hizli register'lar, her 2. tick hucre dizileri + ozet, her 5. tick balans.
/// </summary>
public static class PollSchedule
{
    public static readonly byte[] FastRegisters =
        { Reg.Faults, Reg.Outputs, Reg.PackVoltage, Reg.PackCurrent };

    public static readonly byte[] SummaryRegisters =
    {
        Reg.MaxCellVoltage, Reg.MinCellVoltage, Reg.TotalCellVoltage,
        Reg.MaxCellTemp, Reg.MinCellTemp, Reg.AvgCellVoltage,
        Reg.AvgCellTemp, Reg.MaxSlaveTemp, Reg.EstimatedSoc,
    };

    public const int TickIntervalMs = 100;   // 10 Hz

    public static IReadOnlyList<PollItem> ItemsForTick(long tick)
    {
        var items = new List<PollItem>(5) { PollItem.FastRegisters };
        if (tick % 2 == 0)
        {
            items.Add(PollItem.SummaryRegisters);
            items.Add(PollItem.CellVoltages);
            items.Add(PollItem.CellTemps);
        }
        if (tick % 5 == 0) items.Add(PollItem.Balance);
        return items;
    }
}
```

- [ ] **Adım 4: `PollWorker` yaz** — `BmsUi/Polling/PollWorker.cs`

```csharp
using System.Collections.Concurrent;
using System.Diagnostics;
using BmsUi.Model;
using BmsUi.Protocol;
using BmsUi.Serial;

namespace BmsUi.Polling;

/// <summary>
/// Arka plan thread'i: porta erisen TEK yer. UI'dan gelen yazma istekleri kuyruga
/// alinip poll turlari ARASINDA islenir; boylece iki thread ayni porta yazmaz.
/// </summary>
public sealed class PollWorker : IDisposable
{
    private readonly SerialLink _link;
    private readonly BmsSnapshot _state = BmsSnapshot.Empty();
    private readonly ConcurrentQueue<WriteRequest> _writes = new();
    private readonly double[] _voltBuf = new double[HvProtocol.CellCount];
    private readonly double[] _tempBuf = new double[HvProtocol.CellCount];
    private readonly ushort[] _balBuf = new ushort[HvProtocol.SegmentCount];

    private Thread? _thread;
    private CancellationTokenSource? _cts;
    private int _uiBusy;   // 0 = bosta, 1 = UI guncelleme devam ediyor

    public const int MaxConsecutiveFailures = 5;

    public PollWorker(SerialLink link) => _link = link;

    public event Action<BmsSnapshot>? SnapshotReady;
    public event Action<string>? ConnectionLost;

    private sealed record WriteRequest(byte Index, ushort Value, Action<ushort?> Callback);

    public void EnqueueWrite(byte index, ushort value, Action<ushort?> callback)
        => _writes.Enqueue(new WriteRequest(index, value, callback));

    public void Start()
    {
        _cts = new CancellationTokenSource();
        _thread = new Thread(() => Loop(_cts.Token))
        {
            IsBackground = true,
            Name = "BmsUi Poll Worker",
        };
        _thread.Start();
    }

    public void Stop()
    {
        _cts?.Cancel();
        _thread?.Join(TimeSpan.FromSeconds(2));
        _thread = null;
        _cts?.Dispose();
        _cts = null;
    }

    private void Loop(CancellationToken token)
    {
        long tick = 0;
        var sw = Stopwatch.StartNew();

        while (!token.IsCancellationRequested)
        {
            long dueAt = (tick + 1) * PollSchedule.TickIntervalMs;

            // 1) Bekleyen yazmalar (poll turundan ONCE, tek thread kurali korunur)
            while (_writes.TryDequeue(out var req))
            {
                ushort? echo = _link.WriteRegister(req.Index, req.Value);
                req.Callback(echo);
            }

            // 2) Bu tick'in poll ogeleri
            foreach (var item in PollSchedule.ItemsForTick(tick))
            {
                if (token.IsCancellationRequested) break;
                Poll(item);
            }

            // 3) Baglanti sagligi
            if (_link.ConsecutiveFailures >= MaxConsecutiveFailures)
            {
                ConnectionLost?.Invoke(_link.LastError ?? "Cihaz cevap vermiyor");
                return;
            }

            // 4) UI'ya yayin — onceki guncelleme bitmediyse bu turu atla (kuyruk sismesin)
            if (Interlocked.CompareExchange(ref _uiBusy, 1, 0) == 0)
            {
                var copy = _state.Clone();
                var handler = SnapshotReady;
                if (handler is null) Interlocked.Exchange(ref _uiBusy, 0);
                else handler(copy);
            }

            // 5) Tick hizasi
            int sleep = (int)(dueAt - sw.ElapsedMilliseconds);
            if (sleep > 0) token.WaitHandle.WaitOne(sleep);
            tick++;
        }
    }

    /// <summary>UI, snapshot'i isledigini bildirir; sonraki tur yayin yapabilir.</summary>
    public void NotifyUiIdle() => Interlocked.Exchange(ref _uiBusy, 0);

    private void Poll(PollItem item)
    {
        switch (item)
        {
            case PollItem.FastRegisters:
                foreach (byte idx in PollSchedule.FastRegisters) PollRegister(idx);
                break;

            case PollItem.SummaryRegisters:
                foreach (byte idx in PollSchedule.SummaryRegisters) PollRegister(idx);
                break;

            case PollItem.CellVoltages:
            {
                var frame = _link.Transact(new[] { HvProtocol.CmdCellVoltages },
                                           HvProtocol.CellFrameLength, HvProtocol.CmdCellVoltages);
                if (frame is not null &&
                    FrameParser.TryParseCellVoltages(frame, _voltBuf, out _))
                    _state.SetVoltages(_voltBuf);
                break;
            }

            case PollItem.CellTemps:
            {
                var frame = _link.Transact(new[] { HvProtocol.CmdCellTemps },
                                           HvProtocol.CellFrameLength, HvProtocol.CmdCellTemps);
                if (frame is not null &&
                    FrameParser.TryParseCellTemps(frame, _tempBuf, out _))
                    _state.SetTemps(_tempBuf);
                break;
            }

            case PollItem.Balance:
            {
                var frame = _link.Transact(new[] { HvProtocol.CmdBalance },
                                           HvProtocol.BalanceFrameLength, HvProtocol.CmdBalance);
                if (frame is not null &&
                    FrameParser.TryParseBalance(frame, _balBuf, out _))
                    _state.SetBalance(_balBuf);
                break;
            }
        }
    }

    private void PollRegister(byte idx)
    {
        ushort? v = _link.ReadRegister(idx);
        if (v.HasValue) _state.SetRegister(idx, v.Value);
    }

    public void Dispose() => Stop();
}
```

- [ ] **Adım 5: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test` → tüm testler PASS.

- [ ] **Adım 6: Commit**

```bash
git add BmsUi/Polling BmsUi.Tests/PollScheduleTests.cs
git commit -m "feat(F3): katmanli poll zamanlamasi ve worker thread"
```

---

### Görev 10: Heatmap ve 96-hücre ızgarası

**Dosyalar:**
- Oluştur: `BmsUi/Ui/Heatmap.cs`, `BmsUi/Ui/CellGridControl.cs`, `BmsUi.Tests/HeatmapTests.cs`

**Arayüzler:**
- Üretir:
  - `static Color Heatmap.ForVoltage(double v)`, `static Color Heatmap.ForTemperature(double t)`
  - `enum CellGridMode { Voltage, Temperature }`
  - `sealed class CellGridControl : Control` — `Mode`, `void UpdateData(double[] values, Func<int,bool> isBalancing)`

- [ ] **Adım 1: Başarısız testleri yaz** — `BmsUi.Tests/HeatmapTests.cs`

```csharp
using System.Drawing;
using BmsUi.Ui;
using Xunit;

public class HeatmapTests
{
    [Fact]
    public void ForVoltage_Zero_IsGray()
        => Assert.Equal(Heatmap.InvalidColor, Heatmap.ForVoltage(0.0));

    [Fact]
    public void ForVoltage_BelowUnderVoltage_IsAlarmRed()
        => Assert.Equal(Heatmap.AlarmColor, Heatmap.ForVoltage(2.40));

    [Fact]
    public void ForVoltage_AboveOverVoltage_IsAlarmRed()
        => Assert.Equal(Heatmap.AlarmColor, Heatmap.ForVoltage(4.24));

    [Fact]
    public void ForVoltage_InsideBand_IsNeitherAlarmNorInvalid()
    {
        var c = Heatmap.ForVoltage(3.85);
        Assert.NotEqual(Heatmap.AlarmColor, c);
        Assert.NotEqual(Heatmap.InvalidColor, c);
    }

    [Fact]
    public void ForVoltage_HigherVoltage_IsGreenerThanLower()
    {
        // Bant icinde: 3.2 V mavi ucta, 4.10 V yesil ucta
        Assert.True(Heatmap.ForVoltage(4.05).G > Heatmap.ForVoltage(3.25).G);
        Assert.True(Heatmap.ForVoltage(3.25).B > Heatmap.ForVoltage(4.05).B);
    }

    [Fact]
    public void ForTemperature_AboveThreshold_IsAlarmRed()
        => Assert.Equal(Heatmap.AlarmColor, Heatmap.ForTemperature(81.0));

    [Fact]
    public void ForTemperature_NegativeValue_StaysInBlueEnd()
    {
        var cold = Heatmap.ForTemperature(-10.0);
        Assert.True(cold.B > cold.R);
    }

    [Fact]
    public void ForTemperature_HotterIsMoreRed()
        => Assert.True(Heatmap.ForTemperature(58.0).R > Heatmap.ForTemperature(18.0).R);
}
```

- [ ] **Adım 2: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter HeatmapTests` → derleme hatası.

- [ ] **Adım 3: `Heatmap` yaz** — `BmsUi/Ui/Heatmap.cs`

```csharp
using System.Drawing;

namespace BmsUi.Ui;

/// <summary>
/// Deger -> renk. Normal bantta gradyan, firmware esiklerinin disinda alarm rengi.
/// Esikler: main.h CELL_UNDER/OVER_VOLTAGE_TRESHOLD, CELL_OVER_HEAT_TRESHOLD.
/// </summary>
public static class Heatmap
{
    public const double UnderVoltage = 2.50;
    public const double OverVoltage  = 4.23;
    public const double OverTemp     = 80.0;

    public const double VoltageBandLow  = 3.20;
    public const double VoltageBandHigh = 4.10;
    public const double TempBandLow     = 15.0;
    public const double TempBandHigh    = 60.0;

    public static readonly Color InvalidColor = Color.FromArgb(110, 110, 110);
    public static readonly Color AlarmColor   = Color.FromArgb(210, 40, 40);
    private static readonly Color BandLowColor  = Color.FromArgb(60, 120, 220);   // mavi
    private static readonly Color BandHighColor = Color.FromArgb(70, 190, 90);    // yesil
    private static readonly Color TempHighColor = Color.FromArgb(235, 145, 40);   // turuncu

    public static Color ForVoltage(double v)
    {
        if (v < 0.5) return InvalidColor;                 // eksik / stale hucre
        if (v < UnderVoltage || v > OverVoltage) return AlarmColor;
        return Lerp(BandLowColor, BandHighColor, Normalize(v, VoltageBandLow, VoltageBandHigh));
    }

    public static Color ForTemperature(double t)
    {
        if (t <= -50 || t >= 150) return InvalidColor;    // sensor hatasi
        if (t > OverTemp) return AlarmColor;
        return Lerp(BandLowColor, TempHighColor, Normalize(t, TempBandLow, TempBandHigh));
    }

    private static double Normalize(double value, double low, double high)
        => Math.Clamp((value - low) / (high - low), 0.0, 1.0);

    public static Color Lerp(Color a, Color b, double t) => Color.FromArgb(
        (int)(a.R + (b.R - a.R) * t),
        (int)(a.G + (b.G - a.G) * t),
        (int)(a.B + (b.B - a.B) * t));
}
```

- [ ] **Adım 4: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test --filter HeatmapTests` → PASS (8 test).

- [ ] **Adım 5: `CellGridControl` yaz** — `BmsUi/Ui/CellGridControl.cs`

96 ayrı `Label` yerine tek `Paint`: 6 satır (segment) × 16 sütun (hücre).

```csharp
using System.Drawing;
using System.Drawing.Drawing2D;
using BmsUi.Protocol;

namespace BmsUi.Ui;

public enum CellGridMode { Voltage, Temperature }

/// <summary>6 segment x 16 hucre owner-drawn izgara. Tek Paint, cift tamponlu.</summary>
public sealed class CellGridControl : Control
{
    private readonly double[] _values = new double[HvProtocol.CellCount];
    private readonly bool[] _balancing = new bool[HvProtocol.CellCount];

    public CellGridControl()
    {
        SetStyle(ControlStyles.AllPaintingInWmPaint | ControlStyles.OptimizedDoubleBuffer |
                 ControlStyles.UserPaint | ControlStyles.ResizeRedraw, true);
        BackColor = Color.FromArgb(32, 32, 36);
        Font = new Font("Segoe UI", 8f);
    }

    public CellGridMode Mode { get; set; } = CellGridMode.Voltage;

    /// <summary>UI thread'inden cagrilir (Form1.Invoke icinde).</summary>
    public void UpdateData(double[] values, Func<int, bool> isBalancing)
    {
        Array.Copy(values, _values, HvProtocol.CellCount);
        for (int i = 0; i < HvProtocol.CellCount; i++) _balancing[i] = isBalancing(i);
        Invalidate();
    }

    protected override void OnPaint(PaintEventArgs e)
    {
        var g = e.Graphics;
        g.SmoothingMode = SmoothingMode.None;
        g.Clear(BackColor);

        const int labelWidth = 34;   // sol taraftaki "S1".."S6" etiketleri
        int cols = HvProtocol.CellsPerSegment;
        int rows = HvProtocol.SegmentCount;
        float cellW = (Width - labelWidth - 4f) / cols;
        float cellH = (Height - 4f) / rows;

        using var alarmPen   = new Pen(Color.White, 2f);
        using var balancePen = new Pen(Color.Gold, 2.5f);
        using var gridPen    = new Pen(Color.FromArgb(60, 60, 66), 1f);
        using var textBrush  = new SolidBrush(Color.White);
        using var dimBrush   = new SolidBrush(Color.FromArgb(170, 170, 175));
        using var fmt = new StringFormat
        {
            Alignment = StringAlignment.Center,
            LineAlignment = StringAlignment.Center,
        };

        for (int seg = 0; seg < rows; seg++)
        {
            var labelRect = new RectangleF(2, 2 + seg * cellH, labelWidth, cellH);
            g.DrawString($"S{seg + 1}", Font, dimBrush, labelRect, fmt);

            for (int c = 0; c < cols; c++)
            {
                int index = seg * cols + c;
                double value = _values[index];
                var rect = new RectangleF(labelWidth + 2 + c * cellW, 2 + seg * cellH,
                                          cellW - 1.5f, cellH - 1.5f);

                Color fill = Mode == CellGridMode.Voltage
                    ? Heatmap.ForVoltage(value)
                    : Heatmap.ForTemperature(value);

                using (var brush = new SolidBrush(fill)) g.FillRectangle(brush, rect);
                g.DrawRectangle(gridPen, rect.X, rect.Y, rect.Width, rect.Height);

                if (fill == Heatmap.AlarmColor)
                    g.DrawRectangle(alarmPen, rect.X + 1, rect.Y + 1, rect.Width - 2, rect.Height - 2);
                if (_balancing[index])
                    g.DrawRectangle(balancePen, rect.X + 1, rect.Y + 1, rect.Width - 2, rect.Height - 2);

                string text = Mode == CellGridMode.Voltage
                    ? (value < 0.5 ? "—" : value.ToString("F3"))
                    : value.ToString("F1");
                string caption = $"{index}\n{text}";
                g.DrawString(caption, Font, textBrush, rect, fmt);
            }
        }
    }
}
```

- [ ] **Adım 6: Voltaj ve Sıcaklık sekmelerini Form1'e ekle**

`Form1.Designer.cs`: `SplitContainer mainSplit` (`Dock = Fill`, `FixedPanel = Panel1`,
`SplitterDistance = 320`). `mainSplit.Panel2` içine `TabControl tabs` (`Dock = Fill`),
sekmeler: `voltageTab` ("Voltaj"), `temperatureTab` ("Sicaklik"). Her sekmeye bir
`CellGridControl` (`Dock = Fill`): `voltageGrid` (`Mode = Voltage`),
`temperatureGrid` (`Mode = Temperature`). `voltageTab` altına `Dock = Bottom` bir
`Label`: "Not: firmware'de hucre 94, hucre 20 ile ayni degeri gosterir (main.cpp:2045)."

- [ ] **Adım 7: Worker'ı bağla ve ızgarayı besle** — `Form1.cs`

```csharp
private PollWorker? _worker;

// startButton_Click icinde, ping basarili olduktan SONRA:
_worker = new PollWorker(link);
_worker.SnapshotReady += OnSnapshotReady;
_worker.ConnectionLost += OnConnectionLost;
_worker.Start();

private void OnSnapshotReady(BmsSnapshot snapshot)
{
    if (IsDisposed) return;
    BeginInvoke(() =>
    {
        try { ApplySnapshot(snapshot); }
        finally { _worker?.NotifyUiIdle(); }   // worker'a "hazirim" de
    });
}

private void OnConnectionLost(string reason)
{
    if (IsDisposed) return;
    BeginInvoke(() =>
    {
        statusLabel.Text = $"Baglanti kayip: {reason}";
        statusLabel.ForeColor = Color.Firebrick;
        Disconnect();
    });
}

private void ApplySnapshot(BmsSnapshot s)
{
    voltageGrid.UpdateData(s.CellVoltages, s.IsBalancing);
    temperatureGrid.UpdateData(s.CellTemps, s.IsBalancing);
}
```

`Disconnect()` içine, `_link` kapatılmadan **önce**:

```csharp
_worker?.Stop();
_worker?.Dispose();
_worker = null;
```

- [ ] **Adım 8: Uçtan uca elle test**

`python bms_simulator.py --port COM11` + `dotnet run --project BmsUi` → Start.
Beklenen: Voltaj sekmesinde 96 hücre renkli ızgara, değerler ~3.3-4.19 V arası sürükleniyor;
Sıcaklık sekmesinde 3 hücre belirgin sıcak. Titreme yok (çift tamponlu).

- [ ] **Adım 9: Commit**

```bash
git add BmsUi/Ui BmsUi/Form1.cs BmsUi/Form1.Designer.cs BmsUi.Tests/HeatmapTests.cs
git commit -m "feat(F4): heatmap ve 96 hucrelik owner-drawn izgara"
```

---

### Görev 11: Paket paneli, fault listesi, çıkış ışıkları

**Dosyalar:**
- Oluştur: `BmsUi/Ui/StatusLight.cs`
- Değiştir: `BmsUi/Form1.cs`, `BmsUi/Form1.Designer.cs`

**Arayüzler:**
- Tüketir: `BmsSnapshot`, `FaultBits`, `OutputBits`
- Üretir: `sealed class StatusLight : Control` — `string Caption`, `bool Active`, `Color ActiveColor`

- [ ] **Adım 1: `StatusLight` yaz** — `BmsUi/Ui/StatusLight.cs`

```csharp
using System.Drawing;
using System.Drawing.Drawing2D;

namespace BmsUi.Ui;

/// <summary>AIR / PRE / ERR gostergesi: yuvarlak lamba + etiket.</summary>
public sealed class StatusLight : Control
{
    private bool _active;

    public StatusLight()
    {
        SetStyle(ControlStyles.AllPaintingInWmPaint | ControlStyles.OptimizedDoubleBuffer |
                 ControlStyles.UserPaint | ControlStyles.ResizeRedraw, true);
        Size = new Size(90, 34);
        Font = new Font("Segoe UI", 9f, FontStyle.Bold);
    }

    public string Caption { get; set; } = "AIR";
    public Color ActiveColor { get; set; } = Color.LimeGreen;

    public bool Active
    {
        get => _active;
        set { if (_active != value) { _active = value; Invalidate(); } }
    }

    protected override void OnPaint(PaintEventArgs e)
    {
        var g = e.Graphics;
        g.SmoothingMode = SmoothingMode.AntiAlias;
        g.Clear(Parent?.BackColor ?? BackColor);

        int d = Math.Min(Height - 8, 22);
        var circle = new Rectangle(4, (Height - d) / 2, d, d);
        Color fill = _active ? ActiveColor : Color.FromArgb(70, 70, 76);
        using (var b = new SolidBrush(fill)) g.FillEllipse(b, circle);
        using (var p = new Pen(Color.FromArgb(30, 30, 34), 1.5f)) g.DrawEllipse(p, circle);

        using var textBrush = new SolidBrush(ForeColor);
        using var fmt = new StringFormat { LineAlignment = StringAlignment.Center };
        g.DrawString(Caption, Font, textBrush,
                     new RectangleF(circle.Right + 6, 0, Width - circle.Right - 6, Height), fmt);
    }
}
```

- [ ] **Adım 2: Sol paneli Designer'da kur**

`mainSplit.Panel1` içine yukarıdan aşağıya (`Dock = Top` sırayla, en üstteki en son eklenir):

1. `GroupBox packGroup` ("Paket"), yükseklik 150 — içinde `TableLayoutPanel` (2 sütun):
   `Voltaj`/`packVoltageValue`, `Akim`/`packCurrentValue`, `Guc`/`powerValue`,
   `SoC`/`socValue`, `Toplam hucre V`/`totalCellValue`, `Maks slave sic.`/`slaveTempValue`.
2. `GroupBox statsGroup` ("Hucre istatistikleri"), yükseklik 130 — `minVoltLabel`,
   `maxVoltLabel`, `avgVoltLabel`, `minTempLabel`, `maxTempLabel`, `avgTempLabel`.
3. `GroupBox outputsGroup` ("Cikislar"), yükseklik 60 — `FlowLayoutPanel` içinde
   `airLight`, `preLight`, `errLight` (`StatusLight`).
4. `GroupBox faultsGroup` ("Faultlar"), `Dock = Fill` — `ListView faultsList`
   (`View = Details`, `FullRowSelect`, tek sütun "Durum", `HeaderStyle = None`).
5. `GroupBox healthGroup` ("Baglanti sagligi"), `Dock = Bottom`, yükseklik 70 —
   `healthLabel` (CRC/timeout/kimlik sayaçları + veri yaşı).

- [ ] **Adım 3: Fault listesini bir kez doldur (Form1 kurucusunda)**

```csharp
private void InitFaultList()
{
    faultsList.Items.Clear();
    foreach (string name in FaultBits.Names)
        faultsList.Items.Add(new ListViewItem(name) { ForeColor = Color.DimGray });
    faultsList.Columns[0].Width = -2;   // icerige gore genislik
}
```

- [ ] **Adım 4: `ApplySnapshot`'ı genişlet** — `BmsUi/Form1.cs`

```csharp
private void ApplySnapshot(BmsSnapshot s)
{
    voltageGrid.UpdateData(s.CellVoltages, s.IsBalancing);
    temperatureGrid.UpdateData(s.CellTemps, s.IsBalancing);

    // Paket
    packVoltageValue.Text = $"{s.PackVoltage:F2} V";
    packCurrentValue.Text = $"{s.PackCurrent:F1} A";
    powerValue.Text       = $"{s.PowerKw:F2} kW";     // V x I (firmware idx 41 golgeli)
    socValue.Text         = $"{s.SocPercent:F1} %";
    totalCellValue.Text   = $"{s.TotalCellVoltage:F2} V";
    slaveTempValue.Text   = $"{s.MaxSlaveTemp:F1} °C";

    // Hucre istatistikleri (indeksler host tarafinda hesaplanir — firmware vermiyor)
    var v = s.VoltageStats;
    var t = s.TempStats;
    minVoltLabel.Text = v.HasData ? $"Min V: {v.Min:F3} (hucre {v.MinIndex})" : "Min V: —";
    maxVoltLabel.Text = v.HasData ? $"Maks V: {v.Max:F3} (hucre {v.MaxIndex})" : "Maks V: —";
    avgVoltLabel.Text = v.HasData ? $"Ort V: {v.Avg:F3}  ({v.ValidCount} hucre)" : "Ort V: —";
    minTempLabel.Text = t.HasData ? $"Min T: {t.Min:F1} °C (hucre {t.MinIndex})" : "Min T: —";
    maxTempLabel.Text = t.HasData ? $"Maks T: {t.Max:F1} °C (hucre {t.MaxIndex})" : "Maks T: —";
    avgTempLabel.Text = t.HasData ? $"Ort T: {t.Avg:F1} °C" : "Ort T: —";

    // Cikislar
    airLight.Active = s.AirClosed;
    preLight.Active = s.PreActive;
    errLight.Active = s.ErrActive;

    // Faultlar
    for (int i = 0; i < FaultBits.Names.Length; i++)
    {
        bool active = FaultBits.IsSet(s.Faults, i);
        var item = faultsList.Items[i];
        item.ForeColor = active ? Color.White : Color.DimGray;
        item.BackColor = active ? Color.Firebrick : faultsList.BackColor;
    }

    // Saglik
    double ageMs = (DateTime.Now - s.RegistersAt).TotalMilliseconds;
    healthLabel.Text = $"CRC hata: {_link?.CrcErrorCount ?? 0}   " +
                       $"Zaman asimi: {_link?.TimeoutCount ?? 0}   " +
                       $"Kimlik: {_link?.IdMismatchCount ?? 0}\n" +
                       $"Veri yasi: {ageMs:F0} ms";
}
```

`errLight.ActiveColor = Color.Firebrick;` ve `preLight.ActiveColor = Color.Orange;`
kurucuda ayarlanır (AIR varsayılan yeşil kalır).

- [ ] **Adım 5: Uçtan uca elle test**

`python bms_simulator.py --port COM11 --fault 2 --fault 13` ile başlatın.
Beklenen: "Hucre asiri voltaj" ve "Precharge zaman asimi" satırları kırmızı; ERR ışığı yanar;
~5 sn sonra PRE ve AIR ışıkları yeşile döner; paket voltajı ~370 V civarı, akım salınıyor.

- [ ] **Adım 6: Commit**

```bash
git add BmsUi/Ui/StatusLight.cs BmsUi/Form1.cs BmsUi/Form1.Designer.cs
git commit -m "feat(F5): paket paneli, hucre istatistikleri, fault listesi, AIR/PRE/ERR"
```

---

### Görev 12: Balans sekmesi ve config paneli

**Dosyalar:**
- Değiştir: `BmsUi/Form1.cs`, `BmsUi/Form1.Designer.cs`

**Arayüzler:**
- Tüketir: `PollWorker.EnqueueWrite`, `BmsSnapshot.IsBalancing`, `Reg.*`

- [ ] **Adım 1: Balans sekmesini ekle**

`tabs` içine `balanceTab` ("Balans"). İçine `CellGridControl balanceGrid`
(`Mode = Voltage`, `Dock = Fill`) ve `Dock = Bottom` bir `Label balanceSummary`.
Balans sekmesinde ızgara voltajı gösterir, balanstaki hücreler altın çerçeveyle işaretlidir
(çerçeve mantığı `CellGridControl` içinde zaten var).

`ApplySnapshot` sonuna:

```csharp
balanceGrid.UpdateData(s.CellVoltages, s.IsBalancing);
int balancingCount = 0;
for (int i = 0; i < HvProtocol.CellCount; i++) if (s.IsBalancing(i)) balancingCount++;
balanceSummary.Text = $"Balanstaki hucre: {balancingCount}/96   " +
                      $"Izin verilen dengesizlik: {s.Registers[Reg.AllowedDisbalance]} mV   " +
                      $"(veri yasi {(DateTime.Now - s.BalanceAt).TotalMilliseconds:F0} ms)\n" +
                      "Not: balans ac/kapa firmware'de ayri bir global; UI'dan kontrol edilemez.";
```

- [ ] **Adım 2: Config sekmesini ekle**

`tabs` içine `configTab` ("Config"). `TableLayoutPanel` (3 sütun: etiket, `NumericUpDown`,
buton) — satırlar:

| Etiket | Kontrol | Aralık | Register |
|---|---|---|---|
| "Izin verilen dengesizlik (mV)" | `disbalanceInput` | 1-1000 | `Reg.AllowedDisbalance` (30) |
| "Precharge yuzdesi (%)" | `prechargePctInput` | 50-100 | `Reg.PrechargePercentage` (32) |
| "Precharge zaman asimi (ms)" | `prechargeTimeoutInput` | 100-60000 | `Reg.PrechargeTimeout` (33) |

Altta `Button applyConfigButton` ("Secilenleri yaz") ve `Label configStatusLabel`.
`configTab` altına `Dock = Bottom` uyarı: "Yazilan degerler cihazin RAM'indeki MAINBUFFER'a
gider; guc kesildiginde kaybolur."

- [ ] **Adım 3: Yazma akışını bağla**

```csharp
private void applyConfigButton_Click(object sender, EventArgs e)
{
    if (_worker is null) { MessageBox.Show("Once cihaza baglanin.", "Uyari"); return; }

    var writes = new (byte Index, ushort Value, string Name)[]
    {
        (Reg.AllowedDisbalance,   (ushort)disbalanceInput.Value,      "Izin verilen dengesizlik"),
        (Reg.PrechargePercentage, (ushort)prechargePctInput.Value,    "Precharge yuzdesi"),
        (Reg.PrechargeTimeout,    (ushort)prechargeTimeoutInput.Value,"Precharge zaman asimi"),
    };

    var summary = string.Join("\n", writes.Select(w => $"  {w.Name} = {w.Value}"));
    if (MessageBox.Show($"Asagidaki degerler CALISAN BMS'e yazilacak:\n\n{summary}\n\n" +
                        "Devam edilsin mi?", "Config yazma onayi",
                        MessageBoxButtons.YesNo, MessageBoxIcon.Warning) != DialogResult.Yes)
        return;

    configStatusLabel.Text = "Yaziliyor...";
    foreach (var w in writes)
    {
        var captured = w;
        // Yazma worker thread'inde islenir; geri cagri da orada calisir -> UI'ya Invoke
        _worker.EnqueueWrite(captured.Index, captured.Value, echo => BeginInvoke(() =>
        {
            configStatusLabel.Text = echo == captured.Value
                ? $"{captured.Name}: yazildi ve dogrulandi ({echo})"
                : $"{captured.Name}: DOGRULANAMADI (cihaz {echo?.ToString() ?? "cevap vermedi"})";
            configStatusLabel.ForeColor = echo == captured.Value ? Color.Green : Color.Firebrick;
        }));
    }
}
```

Bağlanıldığında mevcut değerleri kutulara yükle — `ApplySnapshot` içinde, yalnızca kullanıcı
kutuya dokunmadıysa (`_configLoaded` bayrağı):

```csharp
if (!_configLoaded && s.RegisterValid[Reg.AllowedDisbalance])
{
    disbalanceInput.Value       = Math.Clamp(s.Registers[Reg.AllowedDisbalance], 1, 1000);
    prechargePctInput.Value     = Math.Clamp(s.Registers[Reg.PrechargePercentage], 50, 100);
    prechargeTimeoutInput.Value = Math.Clamp(s.Registers[Reg.PrechargeTimeout], 100, 60000);
    _configLoaded = true;
}
```

Config register'ları poll listesinde yok (nadiren değişir). Bağlantı kurulunca bir kez
okumak için `PollSchedule.ItemsForTick(0)` turuna ek olarak `PollWorker.Start()` içinde
şu üçü bir kez sorgulanır:

```csharp
// Loop() basinda, dongu ONCESI:
foreach (byte idx in new[] { Reg.AllowedDisbalance, Reg.PrechargePercentage, Reg.PrechargeTimeout })
    PollRegister(idx);
```

`Disconnect()` içinde `_configLoaded = false;` yapılır.

- [ ] **Adım 4: Uçtan uca elle test**

Simülatör çalışırken Config sekmesinde dengesizliği 20 → 60 mV yapıp "Secilenleri yaz".
Beklenen: onay dialogu çıkar; simülatör konsolunda `YAZ MAINBUFFER[30] = 60`;
UI'da yeşil "yazildi ve dogrulandi (60)"; Balans sekmesinde balanstaki hücre sayısı düşer.

- [ ] **Adım 5: Commit**

```bash
git add BmsUi/Form1.cs BmsUi/Form1.Designer.cs BmsUi/Polling/PollWorker.cs
git commit -m "feat(F6): balans gorunumu ve config yazma paneli"
```

---

### Görev 13: CSV loglama

**Dosyalar:**
- Oluştur: `BmsUi/Logging/CsvLogger.cs`, `BmsUi.Tests/CsvLoggerTests.cs`
- Değiştir: `BmsUi/Form1.cs`, `BmsUi/Form1.Designer.cs`

**Arayüzler:**
- Üretir: `sealed class CsvLogger : IDisposable` — `CsvLogger(string path, TimeSpan interval)`,
  `void Log(BmsSnapshot s)` (hız sınırlı), `static string BuildHeader()`, `long RowCount`

- [ ] **Adım 1: Başarısız testleri yaz** — `BmsUi.Tests/CsvLoggerTests.cs`

```csharp
using BmsUi.Logging;
using BmsUi.Model;
using BmsUi.Protocol;
using Xunit;

public class CsvLoggerTests
{
    [Fact]
    public void BuildHeader_HasAllColumns()
    {
        var cols = CsvLogger.BuildHeader().Split(',');
        // zaman + 96 voltaj + 96 sicaklik + 6 balans + 9 paket alani
        Assert.Equal(1 + 96 + 96 + 6 + 9, cols.Length);
        Assert.Equal("zaman", cols[0]);
        Assert.Equal("v0", cols[1]);
        Assert.Equal("t95", cols[192]);
    }

    [Fact]
    public void Log_RespectsInterval()
    {
        string path = Path.Combine(Path.GetTempPath(), $"hvbms_test_{Guid.NewGuid():N}.csv");
        try
        {
            using (var logger = new CsvLogger(path, TimeSpan.FromMinutes(1)))
            {
                var s = BmsSnapshot.Empty();
                logger.Log(s);
                logger.Log(s);      // araligi doldurmadi -> yazilmamali
                Assert.Equal(1, logger.RowCount);
            }
            var lines = File.ReadAllLines(path);
            Assert.Equal(2, lines.Length);          // baslik + 1 satir
            Assert.StartsWith("zaman,", lines[0]);
        }
        finally { if (File.Exists(path)) File.Delete(path); }
    }

    [Fact]
    public void Log_WritesInvariantDecimalPoint()
    {
        string path = Path.Combine(Path.GetTempPath(), $"hvbms_test_{Guid.NewGuid():N}.csv");
        try
        {
            var s = BmsSnapshot.Empty();
            var volts = new double[HvProtocol.CellCount];
            volts[0] = 3.875;
            s.SetVoltages(volts);

            using (var logger = new CsvLogger(path, TimeSpan.Zero)) logger.Log(s);

            string row = File.ReadAllLines(path)[1];
            Assert.Contains("3.875", row);       // TR yerel ayarinda bile nokta kullanilmali
        }
        finally { if (File.Exists(path)) File.Delete(path); }
    }
}
```

- [ ] **Adım 2: Testin başarısız olduğunu doğrula**

Çalıştır: `dotnet test --filter CsvLoggerTests` → derleme hatası.

- [ ] **Adım 3: `CsvLogger` yaz** — `BmsUi/Logging/CsvLogger.cs`

```csharp
using System.Globalization;
using System.Text;
using BmsUi.Model;
using BmsUi.Protocol;

namespace BmsUi.Logging;

/// <summary>
/// Snapshot'lari CSV'ye yazar. Sayilar InvariantCulture ile yazilir — TR yerel ayarinda
/// virgul ondalik ayraci CSV'yi bozardi.
/// </summary>
public sealed class CsvLogger : IDisposable
{
    private readonly StreamWriter _writer;
    private readonly TimeSpan _interval;
    private DateTime _lastWrite = DateTime.MinValue;

    public CsvLogger(string path, TimeSpan interval)
    {
        _interval = interval;
        bool isNew = !File.Exists(path) || new FileInfo(path).Length == 0;
        _writer = new StreamWriter(path, append: true, Encoding.UTF8) { AutoFlush = true };
        if (isNew) _writer.WriteLine(BuildHeader());
    }

    public long RowCount { get; private set; }
    public string FilePath { get; } = string.Empty;

    public static string BuildHeader()
    {
        var sb = new StringBuilder("zaman");
        for (int i = 0; i < HvProtocol.CellCount; i++) sb.Append($",v{i}");
        for (int i = 0; i < HvProtocol.CellCount; i++) sb.Append($",t{i}");
        for (int i = 0; i < HvProtocol.SegmentCount; i++) sb.Append($",bal{i}");
        sb.Append(",pack_v,pack_a,guc_kw,soc,faults,outputs,min_v,maks_v,ort_v");
        return sb.ToString();
    }

    public void Log(BmsSnapshot s)
    {
        var now = DateTime.Now;
        if (now - _lastWrite < _interval) return;
        _lastWrite = now;

        var ci = CultureInfo.InvariantCulture;
        var sb = new StringBuilder(now.ToString("yyyy-MM-dd HH:mm:ss.fff", ci));
        foreach (double v in s.CellVoltages) sb.Append(',').Append(v.ToString("F3", ci));
        foreach (double t in s.CellTemps) sb.Append(',').Append(t.ToString("F2", ci));
        foreach (ushort b in s.BalanceBitmaps) sb.Append(',').Append(b.ToString(ci));

        var stats = s.VoltageStats;
        sb.Append(',').Append(s.PackVoltage.ToString("F2", ci))
          .Append(',').Append(s.PackCurrent.ToString("F1", ci))
          .Append(',').Append(s.PowerKw.ToString("F3", ci))
          .Append(',').Append(s.SocPercent.ToString("F2", ci))
          .Append(',').Append(s.Faults.ToString(ci))
          .Append(',').Append(s.Outputs.ToString(ci))
          .Append(',').Append(stats.Min.ToString("F3", ci))
          .Append(',').Append(stats.Max.ToString("F3", ci))
          .Append(',').Append(stats.Avg.ToString("F3", ci));

        _writer.WriteLine(sb.ToString());
        RowCount++;
    }

    public void Dispose() => _writer.Dispose();
}
```

- [ ] **Adım 4: Testlerin geçtiğini doğrula**

Çalıştır: `dotnet test --filter CsvLoggerTests` → PASS (3 test).

- [ ] **Adım 5: Log sekmesini ekle**

`tabs` içine `logTab` ("Log"): `Button chooseFileButton` ("Dosya sec..."),
`TextBox logPathBox` (`ReadOnly`), `NumericUpDown logRateInput` (0.1-10 Hz, varsayılan 1),
`CheckBox logEnabledCheck` ("Kaydi baslat"), `Label logStatusLabel`.

```csharp
private CsvLogger? _logger;

private void logEnabledCheck_CheckedChanged(object sender, EventArgs e)
{
    if (logEnabledCheck.Checked)
    {
        if (string.IsNullOrWhiteSpace(logPathBox.Text))
        {
            MessageBox.Show("Once bir dosya secin.", "Uyari");
            logEnabledCheck.Checked = false;
            return;
        }
        var interval = TimeSpan.FromSeconds(1.0 / (double)logRateInput.Value);
        _logger = new CsvLogger(logPathBox.Text, interval);
        logStatusLabel.Text = "Kayit suruyor...";
    }
    else
    {
        logStatusLabel.Text = _logger is null ? "" : $"Kayit durdu ({_logger.RowCount} satir)";
        _logger?.Dispose();
        _logger = null;
    }
}
```

`ApplySnapshot` sonuna `_logger?.Log(s);` ve `Disconnect()` içine
`logEnabledCheck.Checked = false;` eklenir.

- [ ] **Adım 6: Commit**

```bash
git add BmsUi/Logging BmsUi.Tests/CsvLoggerTests.cs BmsUi/Form1.cs BmsUi/Form1.Designer.cs
git commit -m "feat(F7): CSV loglama"
```

---

### Görev 14: Yeniden bağlanma, tema, README

**Dosyalar:**
- Değiştir: `BmsUi/Form1.cs`, `BmsUi/Form1.Designer.cs`
- Oluştur: `README.md`

**Arayüzler:**
- Tüketir: mevcut her şey. Yeni public API yok.

- [ ] **Adım 1: Otomatik yeniden bağlanma**

`Form1`'e `CheckBox autoReconnectCheck` ("Otomatik yeniden baglan") ve
`System.Windows.Forms.Timer reconnectTimer` (`Interval = 3000`) ekleyin.

```csharp
private string? _lastPortName;

private void OnConnectionLost(string reason)
{
    if (IsDisposed) return;
    BeginInvoke(() =>
    {
        statusLabel.Text = $"Baglanti kayip: {reason}";
        statusLabel.ForeColor = Color.Firebrick;
        Disconnect();
        if (autoReconnectCheck.Checked && _lastPortName is not null) reconnectTimer.Start();
    });
}

private void reconnectTimer_Tick(object sender, EventArgs e)
{
    if (_link is not null) { reconnectTimer.Stop(); return; }
    if (!System.IO.Ports.SerialPort.GetPortNames().Contains(_lastPortName)) return;

    statusLabel.Text = $"{_lastPortName} icin yeniden deneniyor...";
    RefreshPorts();
    portCombo.SelectedItem = _lastPortName;
    startButton_Click(this, EventArgs.Empty);
    if (_link is not null) reconnectTimer.Stop();
}
```

`startButton_Click` içinde bağlantı kurulunca `_lastPortName = portName;` atanır ve
`reconnectTimer.Stop();` çağrılır.

- [ ] **Adım 2: Koyu tema**

`Form1` kurucusunda, `InitializeComponent()` sonrası:

```csharp
private static readonly Color ThemeBack = Color.FromArgb(28, 28, 32);
private static readonly Color ThemePanel = Color.FromArgb(38, 38, 44);
private static readonly Color ThemeText = Color.FromArgb(232, 232, 236);

private void ApplyTheme(Control root)
{
    root.BackColor = root is GroupBox or Panel or TabPage ? ThemePanel : root.BackColor;
    root.ForeColor = ThemeText;
    foreach (Control child in root.Controls) ApplyTheme(child);
}
// kurucuda:
BackColor = ThemeBack;
ForeColor = ThemeText;
ApplyTheme(this);
faultsList.BackColor = ThemePanel;
faultsList.ForeColor = ThemeText;
```

- [ ] **Adım 3: README yaz** — `README.md`

Bölümler: ne işe yarar · gereksinimler (.NET 10 SDK, Windows, VSPE/com0com) ·
`dotnet build -c Release` / `dotnet run --project BmsUi` / `dotnet test` ·
simülatörle donanımsız kullanım (`python bms_simulator.py --port COM11`, bayraklar) ·
protokol özeti tablosu (komut → cevap uzunluğu) · MAINBUFFER indeks tablosu ·
FAULTS bit listesi · bilinen kısıtlar (balans kontrolü firmware'de yok, hücre 94 = hücre 20
remap'i, idx 41/42/43 gölgeli, SoC firmware'de şu an 0).

- [ ] **Adım 4: Tam doğrulama**

Çalıştır:

```bash
dotnet build -c Release
dotnet test
```

Beklenen: build uyarısız, tüm testler PASS.

Sonra simülatörle 5 dakikalık dayanıklılık koşusu:

```bash
python bms_simulator.py --port COM11 --chunked --latency 3 --fault 6
```

Beklenen: UI kesintisiz güncellenir, CRC/zaman aşımı sayaçları 0'da kalır, veri yaşı
sürekli < 300 ms, "Hucre asiri sicaklik" kırmızı. Simülatörü kapatınca ~0.5 sn içinde
"Baglanti kayip" görünür; tekrar başlatınca otomatik yeniden bağlanır.

- [ ] **Adım 5: Commit**

```bash
git add -A
git commit -m "feat(F7): otomatik yeniden baglanma, koyu tema, README"
```

---

## Öz-inceleme notları

- **Spec kapsamı:** UI gereksinim 1 → Görev 7; 2 → Görev 10; 3 → Görev 11 (indeksler host'ta
  hesaplanıyor); 4 → Görev 11; 5 → Görev 11; 6 → Görev 12; 7 → Görev 11; 8 → Görev 12 (config)
  ve Görev 13 (CSV). Protokol → Görev 2-5; simülatör → Görev 6; poll worker → Görev 9;
  hata yönetimi → Görev 5 (sayaçlar), 9 (bağlantı kaybı), 14 (yeniden bağlanma).
- **İsim tutarlılığı:** `SerialLink.Transact/ReadRegister/WriteRegister`,
  `FrameParser.TryParse*`, `BmsSnapshot.SetVoltages/SetTemps/SetBalance/SetRegister`,
  `CellGridControl.UpdateData`, `PollWorker.EnqueueWrite/NotifyUiIdle` — tüm görevlerde aynı
  imzalarla kullanıldı.
- **Bilinçli sadeleştirme:** `PollWorker`'ın kendisi için birim testi yok (thread + gerçek
  zaman); test edilebilir karar mantığı `PollSchedule`'a çıkarıldı, kalan ince sarmalayıcı
  simülatörle uçtan uca doğrulanıyor.
